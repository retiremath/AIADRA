# AIADRA

> Open-source AI-native platform for product engineering. See [Manifesto](Docs/Manifesto.md).

## At session start

1. **Read the most recent file in [Docs/Snapshots/](Docs/Snapshots/)** — it contains current state, in-flight work, and the immediate next step. Snapshots are git-ignored; if the folder doesn't exist on a fresh clone, the project is at v0 with no session-passed state.
2. **Read [Docs/SystemState.md](Docs/SystemState.md)** — curated whole-system navigation / cache layer; ~one page; surfaces the current front, active patterns, Coherence Checklist, load-bearing-now, deferred items, and recent pattern changes. NOT an authority layer (decisions live in ADRs); the cache that prevents zooming-into-one-arc-and-losing-the-big-picture.
3. **Read [Docs/Discussions/Transfer/PROTOCOL.md](Docs/Discussions/Transfer/PROTOCOL.md)** if present — Claude↔Codex coordination protocol established in arc 20260519-1. Defines arc-state-from-file-presence, INBOX regeneration discipline, dispute path, and the Codex write boundary. Git-ignored / local-only; if absent on a fresh clone, the project predates the protocol (treat as v0 coordination — Petre relays manually). Codex extension reads this same file at its session-start tick.
4. **Skim [Docs/Manifesto.md](Docs/Manifesto.md) and [Docs/Glossary.md](Docs/Glossary.md)** if any term in the snapshot or SystemState is unfamiliar.
5. **The auto-memory index** at `~/.claude/projects/d--VSCode-Work/memory/MEMORY.md` is loaded automatically and contains cross-session preferences, ownership rules, and working style — see those memories for the durable rules below in long form.

## Per-arc ritual

Adopted in arc 12. Three checkpoints to maintain whole-system coherence as the front widens:

- **Arc open** (before drafting Claude1): read [SystemState.md](Docs/SystemState.md). ~2 min.
- **Codex review**: Codex explicitly walks the local proposal against SystemState's [Coherence Checklist](Docs/SystemState.md#3-coherence-checklist). ~2 min Codex-side.
- **Arc close** (before commit): ask "Does SystemState need an update?" Update if a pattern, invariant, current front, or deferred item changed. ~5 min if yes; 30 sec if no.

Pattern-setting ADRs usually need an update; small refinements often don't.

## What this project is

AIADRA is an open-source platform where AI agents help design and engineer real products against a single deterministic source of truth. Probabilistic AI proposes and explains; the deterministic core validates and records; humans approve. See [Manifesto.md](Docs/Manifesto.md) for principles and non-goals.

This is a **long, deliberate** project. Years, not weeks. Quality, stability, and architectural coherence beat velocity.

## Working style (short)

Discuss → brainstorm → organize → plan → architect → code. No production code until the design is settled enough for the bit being coded; **spike code is allowed in parallel with design** to stress-test assumptions; production code waits. When in doubt, surface the trade-off and ask — don't barrel ahead on assumptions. (Full rule: `aiadra-working-style` memory.)

## Ownership (short)

Claude is the lead on strategy, planning, and implementation. Other AIs (Codex/ChatGPT) review and advise via files in [Docs/Discussions/](Docs/Discussions/), but Claude owns the synthesis and the rationale — engage seriously, take what improves the project, push back on what doesn't, never rubber-stamp. (Full rule: `aiadra-ownership` memory.)

## Repo layout

```
AIADRA/
├── CLAUDE.md                      # This file — session-start orientation
├── README.md
├── .gitignore
└── Docs/
    ├── Manifesto.md               # Durable project thesis + principles + non-goals (in git)
    ├── Glossary.md                # Pinned term definitions (in git)
    ├── ArchitectureOverview.md    # Layer model (in git)
    ├── TruthModelSchema.md        # Ring 1 abstract Truth Model Schema spine (S0–S3) + Promotion Rule for first-class Object Types (in git)
    ├── ArchitectureGraph.json     # On-demand visualization snapshot of the Overview (in git)
    ├── OpenQuestions.md           # Register of unresolved questions (in git)
    ├── SystemState.md             # Curated navigation / cache layer; read at arc open (in git)
    ├── ADR/                       # Architecture Decision Records (in git)
    ├── Discussions/               # Inter-AI dialogue, dated subfolders (GIT-IGNORED)
    └── Snapshots/                 # Session-end state for handoff (GIT-IGNORED)
```

## Discussion folder convention

`Docs/Discussions/YYYYMMDD/<Source><N>.md` — e.g., `Docs/Discussions/20260517/Claude2.md`, `GPT1.md`. Each session-day gets its own folder; each AI's contributions are numbered sequentially. Git-ignored — local-only.

## Snapshot folder convention

`Docs/Snapshots/YYYY-MM-DD-NN.md` — written by Claude at end of session or after milestones. Captures: status, done this session, in flight, immediate next step, open threads. Git-ignored — local-only. Schema is established in the first snapshot.

## Active document versions

Update this list when versions change.

- [Manifesto.md](Docs/Manifesto.md) — v0.4 (AIAD positioning + Native Engine posture per arc 20260531-11 [ADR/0027](Docs/ADR/0027-aiad-positioning-and-native-engine-posture.md): §"What AIADRA is" reframed for AIAD platform + Native Engines using third-party kernels as libraries; non-goal at "integration wrapper" line rewritten; Principle 12 reworded; About-this-document terms list updated.)
- [Glossary.md](Docs/Glossary.md) — v0.26 (Workspace Browser amended to **AIADRA Studio primary / VSCode secondary** per [ADR/0032](Docs/ADR/0032-aiadra-studio-scope.md) arc 20260602-5. Continues from v0.25, per arc 20260531-11 [ADR/0027](Docs/ADR/0027-aiad-positioning-and-native-engine-posture.md): AIADRA entry reframed for AIAD platform positioning; NEW "AIAD" entry as system category; "Domain Engine" entry replaced with "Native Engine" entry; "Domain Adapter" entry DROPPED + replaced with "Data Adapter" entry; Workspace + Workspace Browser reworded for Native Engine sessions. Continues from v0.24: Wedge entry singular-count shorthand clarified per ADR/0023; new TestExecution Object Type entry per ADR/0022; three new execution-instance relationship-type entries.)
- [ArchitectureOverview.md](Docs/ArchitectureOverview.md) — v0.2 (Layer 5 reframed per arc 20260531-11 [ADR/0027](Docs/ADR/0027-aiad-positioning-and-native-engine-posture.md): "Domain Engines" → "Native Engines + Data Adapters"; Ring 3 framing updated to "Native Engine Implementation contract + Data Adapter integration pattern"; Workspace bullet + five-layer historical framing reworded.)
- [TruthModelSchema.md](Docs/TruthModelSchema.md) — v0.8 (Ring 1 abstract spine **complete** with S0+S1+S2+S2.5+S3 pinned; Promotion Rule for first-class Object Types pinned with 12 commitments including amended commitment 6; **S0 commitment 6 amended per ADR/0008 with cross-project identity tuple, identity-locator split, revision content hash for fixed cross-project bindings, and direct-endpoint policy**; same stale-when-overridden authority as ArchitectureOverview)
- [ArchitectureGraph.json](Docs/ArchitectureGraph.json) — v0.1 (on-demand visualization snapshot; prose Overview is master, graph drifts between refreshes)
- [OpenQuestions.md](Docs/OpenQuestions.md) — v0.8 (OQ-0007 `under-investigation` → `resolved` per arc 20260530-1: Wedge-001 spike ran end-to-end clean and friction log confirms basic Wedge architecture survived contact with reality cleanly. Continues from v0.7: OQ-0007 transitioned `accepted-as-unresolved` → `under-investigation` per ADR/0023; OQ-0016 resolved by ADR/0008 — cross-project Object identity; OQ-0015 resolved by ADR/0004 — Reservation file shape.)
- [SystemState.md](Docs/SystemState.md) — v1 updated through arc 20260715-4 (**SK-C0 BUILT + CLOSED — arcs/circles/construction on the frozen sketch model; Codex6 signoff**: bulge arcs (one pinned convention; v1 minor-only with the angle-space semicircle gate — exact-semicircle clicks refuse), curve-aware Class-1, atomic construction guides, circle-as-outer, ONE `classify_sketch` (empty = valid sketch-only), by-construction wall authority, signature = topology_contributing only, golden `topo_8b3d982e8ef30128`; adapter **0.1.9**; Studio sketch tools + TS mirrors (circumcenter-exact three-point arc; classifier = documented eligibility projection) + engine-grammar fail-loud decode + honest mock (entry-A consumes the classifier; canned fallback deleted). Petre's acceptance: dev-lane feature walk on the post-C0-2/Claude5 working tree. mech 200/200 · studio 381/381 combined tree + tsc + build. NEXT: V arc convergence → SK-C1 slice 1 [references → line+auto-dim → strong-dim edit → regenerate].) Prior: arc 20260715-3 (**SK-B CLOSED — the sketch solver SELECTED: ADR/0044 Amendment A1, the patched PlaneGCS extraction as a separate replaceable LGPL DLL behind the AIADRA-owned binding; three face conditions — clean-machine kit release-gated per Petre arbitration · Linux second-platform · attorney review; `tangent_at` joins the SK-C1 palette; the own baseline is the permanent corpus reference; the `skb-1` machine-checked gate + both harnesses live in the arc folder for SK-C1 adoption. Petre's standing method: one sketcher behavior at a time, benchmarked vs Creo 10 side-by-side until satisfied, locked, then next.**) Prior: v1 updated through arc 20260711-11 slice 1a (**the Studio Ring-2 authoring WRITE bridge — [ADR/0043](Docs/ADR/0043-studio-ring2-authoring-bridge.md) ACCEPTED + built + proven**): the interactive-modeling foundation (Petre: *"create geometry manually and with AI… we need the geometry engine figured out"*). The engine ops existed but the Studio bridge was read-only; ADR/0043 pins a **session-capability write surface** — main mints an opaque `operationSessionId`, allowlists feature kinds + validates params at the boundary, brokers verbs (`opBegin`/`opAdd`/`opSimulate`/`opCommit`/`opRollback`); the Ring-2 draft lives in `bridge.py` keyed by that id (no paths/drafts/OCCT handles to the renderer); commit returns refreshed display + closes only on success; rollback = named discard; bridge-exit clears sessions (D4). Proven end-to-end: `scripts/authoring-smoke.py` (create_part→sketch→extrude→commit → 6-face box + discard lifecycle) through the REAL engine. Codex1 design + Codex2 B1 lifecycle fix absorbed. tsc clean; no aiadra-core/schema/bundle change. **Arc 20260711-11 stays OPEN**; slice 1b/1c BUILT + committed — the **clickable manual extrude** (dual-lane `AuthoringBackend` [`dev:web` honest mock + Electron bridge over the ADR/0043 verbs] + a pure `feature` operation session + a floating extrude dashboard → a real 6-face plate through the bridge); Codex3 B1 lifecycle absorbed (the slice-1a no-orphan invariant now reaches the renderer: begin rolls back a partial op sequence; the dashboard retains the session id across commit/error/cancel; cancel guarded during the terminal commit; +3 `backendBridge` tests → **174 vitest**). Then Petre steered to the real Creo/FreeCAD workflow (draw a contour → extrude it). **Slice A** (Creo shell: Model ribbon + tree) + **slice E** (the **arbitrary-contour extrude** engine — a `contour` closed-ring profile lifts the rectangle-only limit; N segment-anchored walls; ADR/0035 identity; Class-1-gated + one-wall-per-segment bijection; `aiadra-mechanical adapter_schema_version` **0.1.5→0.1.6** additive; KB contour bundle; 140 tests; Codex4 contract + Codex5 build both approved) BUILT + committed. **S/X BUILT + committed** — the first DRAWN geometry (2D sketch pad on XY → explicit ring closure → Extrude; dev:web synthesizes the drawn solid; Codex6 B1 ONE shared `sessionLifecycle` for all authoring surfaces + B2 exact Class-1 zero-length parity + mock defence-in-depth; **195 vitest**). Petre benchmarked Creo 10's startup → the **Home-state shell** design Codex6-approved (two app states; Home = workspace browser + recents + AI entry + Catalogs & KB; contextual ribbon + File menu; D-H4 recents repinned per B3: durable main-owned registry, stable `recentId`, reopen = renewed grant + fresh `workspaceId`). **The Home-state shell BUILT + committed (arc 20260714-1, Codex4 SIGNOFF, 221 vitest):** Creo-paradigm Home boot (workspace browser + main-owned recents + AI entry), contextual ribbon + File menu, the New dialog (explicit provisional Number per ADR/0004; session-scoped metadata), ONE gated atomic async workspace transition (published to all op-start gates; acked-or-retained capability retirement), desktop-never-mocks lanes. **Arc 20260714-2 (EMPTY-PART paradigm) design SIGNED OFF:** EP0 Core empty-Part display + ADR/0035 A4 (`empty:v1`, core→0.14.0) → EP2 discriminated sketch-plane binding (`effective_plane_frame`, `resolve_consumed_sketch`, `normal±`; adapter→0.1.7) → EP1 Studio scaffold (commit-at-New; origin + three datum planes; tree; pick-plane sketch). **EP0+EP2 BUILT + committed (Codex4 SIGNOFF):** aiadra-core **0.14.0** + ADR/0035 **A4** (the Part-only kernel-neutral empty display, `empty:v1`, proven via public Ring-2 APIs) · adapter **0.1.7** (the sketch-plane binding: `recipe.py` shared pure module, discriminated `plane` record, exact named-sketch resolution everywhere incl. the signature, `normal±` with bounded legacy `z±`, full 3-plane matrix; roles unrenamed; legacy byte-identity) · KB 0.6.0 · **core 416 / mech 167**. **EP1 BUILT + committed (Codex6 SIGNOFF; Petre runtime-verified):** commit-at-New via a gated cleanup-owning one-shot; the datum scaffold (FRONT/RIGHT/TOP + origin, `intrinsic-*` overlay ids, `scene.datums`); pick-plane Sketch; features on the active Part with a fail-closed target; **234 vitest**. **SK-A CLOSED (arc 20260715-2, Codex3 signoff): [ADR/0044 — The AIADRA Sketcher Paradigm](Docs/ADR/0044-sketcher-paradigm.md) ACCEPTED** — a sketch becomes a CONSTRAINED MODEL (entities/constraints/dimensions/references as facts; solved coordinates derived; v2 = a new incompatible adapter series, v1 coexists forever); the solver-entity/macro/mode ontology; three orthogonal dimension axes (strength/role/provenance; the SK-C1 per-parameter-provenance schema extension named); the two-layer determinism commit invariant (canonical weak completion + persisted branch seed; solver CONTRACT version; typed diagnostic taxonomy); the SK-B evidence-gated solver decision (PlaneGCS LGPL extraction candidate w/ capability+compliance gates + own-solver baseline; libslvs GPLv3 denied); the AI-proposes/solver-validates/human-asserts posture (BYO-AI holds). Roadmap SK-B∥SK-C0 → C1 → C2 → D → E → F → G → H. Design arc — ADR/0044 is the sole git artifact. NEXT: SK-B (solver decision) + SK-C0 (arc/circle/construction on the frozen model). Prior: **R BUILT + committed (arc 20260715-1 CLOSED, Codex4 signoff):** the Model-ribbon benchmark — the full Creo Model-tab vocabulary (38 commands) as a data-driven three-state taxonomy (working / state-disabled-with-derived-reason / roadmap-disabled-with-strand; exact inventory fixture; lane-aware honest mock); the rectangle sketch tool (discriminated `SketchTool`; the engine's native primitive; the whole-list `simple_rectangle` classifier); Revolve (ONE `baseFeature` session, exact straddle-rule axis eligibility, chained `[rectangle, revolve($fromOp)]`, mock lathe parity); Round/Chamfer over canonical selection (generation-bound `selectorFacts` at display install; fail-closed `captureSelectorTarget`; capture-is-session-state); Hole (`holeBaseRefusal` P1 base domain; face-on-current-display; wall-vs-cap engine-authoritative); edit-dimension (version-guarded identity-preserving catalogue; the tree's ✎); the STACKING CONTAINMENT (`hasReferencingFeature`/`stackingRefusal` — the found engine gap "stacked referencing features" is the named follow-up arc: mid-fold `resolve_edge_on_shape` lacks prior ProducedFaceHints); and the bridge simulate-report bug fix (status vs result — simulate could never report invalid). **Studio 301 vitest + tsc + build + assert-no-fixtures · mech 174 · core 416**; no engine/core source change. NEXT: the sketch-refinement strand (Petre's pinned priority) / stacked referencing features / EP3 datums / sequential+cut extrudes. Prior: **S2 BUILT + committed (arc 20260714-3 CLOSED, Petre-signed close after Codex2 design signoff + Codex3/4/5 focused repair rounds; Petre runtime-verified the full dev:web walk):** the sketch-first-class modeling flow — the Truth-fed Creo tree (`Sketch N` unconsumed / `Extrude N` → nested `Section N`), stepwise sketch (OK commits the sketch ALONE → wire overlay `sketch-wire:<id>`, recipe-derived, no contract amendment), dual-entry Extrude (selected sketch → depth; or pick/create with the chained one-draft `[sketch, extrude($fromOp)]`; rectangle FeatureDashboard RETIRED), the ENGINE-owned staged-identity handshake (per-op `created_feature_ids` from draft event deltas; `$fromOp` aliases resolved inside the AuthoringBackends; all renderer `feat_NNNN` prediction dead; `authoringGuards.ts` register-first/awaited-rollback begin settlement), ONE generation-owned `partContext` (ready = the JOIN of display+inspect; synchronous selection clears at the boundary; the `{workspaceId, partNumber, generation}` terminal authority tuple; ONE `authoringGate`; the joined pre-mount `pendingDisplay` coordinator), the engine+version-guarded `inspectDecode` (mechanical-only interpretation; foreign/unknown stay generic), ONE discriminated `authoringSession` store, engine same-kind one-base guards (handler + evaluator; adapter **0.1.7→0.1.8**), and the **no-base display/HLR** (features-but-no-base → zero render/HLR, `pickable_kinds: []`, REAL live identity — deliberately distinct from `empty:v1`; the stepwise paradigm's engine consequence). **Studio 277 vitest + tsc + build + assert-no-fixtures · mech 174 · core 416.** NEXT: Petre's desktop walk; then in-viewport plane/wire picking / live extrude preview / orient-into-sketch / fillet+chamfer over selection / tree affordances; then the AI path (MVP-2 accept→commit). Prior: arc 20260711-10 (**MVP-1 — the fast-lane AIAD authoring loop BUILT** (first working slice of the AIAD authoring model; first Studio code since 6c; pure Studio-consumer — no core/engine/contract/schema/ADR change): a clickable, hot-reloading `dev:web` loop (elicit → propose/show real distinct candidates → refine) realizing ADR/0039 (configurator loop; three-state candidate model) + ADR/0040 (operation session; CAD↔AI dock; provenance-as-UI-primitive). NEW `operation/store.ts` (single-source-of-truth operation session, pure actions), `authoring/brackets.ts` (scripted `generate` configurator), `operation/{candidateSource,previewController}` (Codex B2 — pure store + monotonic-race preview controller), `dock/Dock.tsx` (resizable/dismissable CAD↔AI dock), `operation/SessionPill.tsx` (Codex B1 — actionable hidden-session pill). Candidates preview **real engine-baked geometry** (3 bracket variants baked through OCCT into `dev-fixtures/bracket-*.json`, dispatched by `sourceId`; assert-no-fixtures green). A local `.vscode/tasks.json` one-click dev button was added (`.vscode/` git-ignored → local-only). Three-round (Codex1 design B1/B2 → Claude2 build → Codex2 B1 distinct-geometry → Claude3 bake + close). aiadra-studio **164 vitest** (147→164) + tsc + build. NEXT: MVP-2 (accept→commit via Ring 2 + PDM, the bridge lane). Prior: arc 20260711-9 (**ADR/0042 — Assembly Geometry Composition & Associativity ACCEPTED** (write-down backlog #4 — the LAST; **the ADR write-down backlog is COMPLETE**, ADR/0039–0042 landed; scope ADR, no engine/core code, no schema/bundle change): the scope ADR for *"edit a Float Part → the Assembly updates."* Pins **v1 = positioned composition** (Assembly = component occurrences carrying `occurrence.transform`; assembly geometry = each component's own recipe geometry placed by its transform); occurrence identity + placement **already decided** (ADR/0007/0010) so no schema change; **pull/lazy — no stored Assembly BREP** (recompose from the resolved `composed_of` closure on read); **the recursive aggregate display identity** (a sub-Assembly participates in its parent exactly as a Part does; scoped iff-invariant → the child/placement cascade is automatic; derived-not-Truth); read-vs-write/release closure; the **`derived_geometry_from`** named v1 boundary (deferred); no mate solver. Substrate the **`compose`-configurators (ADR/0039 D4) depend on**. Two-round; Codex1 close-ready + B1 scoped the D4 invariant. **NEXT: the physical-testing MVP build (ADR/0040 D10) + the standing SystemState full-compression.** Prior: arc 20260711-8 (**ADR/0041 — KB Interchange & Ecosystem ACCEPTED** (write-down backlog #3; vision/scope, no engine/core code): makes the two-tier KB shareable/tradeable (~90% reuse). Pins the **KB pack** (git-repo/tarball → one canonical mounted tree digest), **import = read-only digest-locked `pack:` source tier** (fills ADR/0037 A1.7), the **two-group import lock** (content lineage + *separate* license/import state), **licensing = data-not-code directional** (own license, no AGPL conflict expected *subject to attorney review*; executables out of scope), the **data-only prompt-injection-hardened trust boundary** (no executable content; pack prose = untrusted content never instructions; overrides namespace-scoped; canonical archive digest), and **marketplace = ECOSYSTEM-not-Core** (P11). Co-lands the ADR/0037 A1.7 pointer + **ADR/0034 Attorney-review #6** (commercial-pack distribution / data-not-code — release-prerequisite). Two-round; Codex1 close-ready + B1 co-landed ADR/0034 item. Prior: arc 20260711-7 (**ADR/0040 — AIADRA Studio: Application Shell, AI Integration & PDM UX ACCEPTED** (write-down backlog #2; vision/scope, no engine/core code): the application-phase sibling of ADR/0033 (Display step 6 now COMPLETE + consumed; a one-line ADR/0033 pointer co-landed). Consolidates arcs 20260711-3 (Studio app/AI/PDM vision) + 20260711-4 (the CAD↔AI dock, folded into D5). Pins: one interface / three surfaces (app shell + CAD↔AI + PDM); Electron reaffirmed; **the operation session** (manual dashboard + AI panel = two views of ONE shared session store, strict single-source-of-truth, single explicit Ring-2 accept; the UI home of the three-state candidate model); **the CAD↔AI dock** (resizable/dismissable/multi-tab, right-default; dismiss-mid-session presence invariant + stateful toggle; tab-isolation; chrome = local pref; Catalogs & KB local-first); the three-affordance AI surface; **PDM = GitHub-as-PDM, OPTIMISTIC v1** (not pessimistic-via-ADR/0004-Reservation; ADR/0001 rejected live locks); provenance-as-UI-primitive; P-U1…P-U8; a build roadmap led by the **physical-testing MVP** (thin vertical slice, scripted-AI-first). Two-round; Codex1 close-ready + B1 co-landed ADR/0033 pointer + a public-wording watch. Prior: arc 20260711-6 (**ADR/0039 — The AIAD Authoring Model ACCEPTED** (first ADR since ADR/0038; vision/scope, no engine/core code): the authoring peer of ADR/0033. Pins the configurator-as-asset reframe (durable asset = parametric recipe + intent, not geometry), the interaction loop (elicit→retrieve→propose/show→refine→accept→capture), the **three-state candidate model** (candidate recipe → evaluated display → committed Truth; no mockups, transient until Ring-2 accept), **configurator = KB artifact referencing Truth, NOT a Truth-Model Object Type** (v1) + promotion rule, the layered validity domain, the acceptance-bundle capture discipline, principles P-A1…P-A8, **two-tier KB storage** (core-tier packaged + project-tier git-tracked; **content-addressed digest lock** distinct from `compatible:` ranges; ranking-vs-suppression; source namespaces `core:<engine>:`/`project:`/reserved `user:`; fork-to-customize), Ring-5 BYO-AI orchestration, and the generate-first roadmap. **Co-lands ADR/0037 Amendment A1** (KB manifest gains the `configurator` content class + core/project tier + `.aiadra/kb-lock.yaml` project-lock vocabulary + source namespaces + `override`/`origin` + canonical units; the `pack:` interchange tier deferred to a forthcoming ADR). Two-round; Codex1 B1 content-lock-not-version-pin + B2 concrete D13 co-landed patch set; N1–N4. **Opens the ADR write-down backlog** (Studio app/AI/PDM vision + the CAD↔AI dock; KB interchange & ecosystem; assembly geometry composition — then the physical-testing MVP). Prior: arc 20260625-1 **Display 6c — interaction shell BUILT; Display & UX strand step 6 COMPLETE** (ADR/0033 D9/D10; no ADR change — pure Studio consumer): single-select faces+edges (hover imperative/rAF; new `selection/store.ts`; face/edge filter) + standard views + a full interactive nav cube, sharing ONE pure `display/viewOrientation.ts` table (33/33 orthonormal; cube faces ≡ standard views). Ephemeral-identity firewall held: selection resolves only to canonical `face_id`/`edge_id` (`pickTargetsFiltered`; overlay/imports unpickable, test-enforced). Codex B1: the nav cube is an input-owned island (capture-swallows every pointer button + contextmenu + wheel; only a real main-camera move feeds settle; renderer state saved/restored). Codex B2: wireframe face highlight via contract `EdgePolyline.faces` adjacency (not id parsing); selection clears on new package. Manual-test pass fixed a DPI render bug (`navCubeViewportRect` returns CSS px — three.js applies pixelRatio) + finalized the cube to the FreeCAD look (chamfered facets = the pick regions, axis triad gnomon, control ring: 90° yaw/pitch/roll). NEW theme key `hoverHighlight`; NEW command groups `orientation`+`selection`. Three-round; Codex3 signoff. aiadra-studio **147 vitest** (122→147) + tsc + build + `[assert-no-fixtures] ok`; aiadra-core/mechanical UNTOUCHED; NO contract/schema/bundle/Glossary/ADR change. Prior: arc 20260622-4 (**Revolve BUILT — the first non-referencing CREATION feature since extrude** (ADR/0037 D8; no ADR change): `mechanical.add_revolve_feature` sweeps the XY rectangle 360° around a global X/Y axis → a tube/washer or solid cylinder; no ADR/0038 reference (revolve creates base geometry, correlated recipe-first). Spike de-risk: a rectangle-profile revolve is plane+cylinder only → v1 reuses the existing correlation vocabulary, cones/tori = a deferred richer-profile v2. Base build is now sketch → **extrude XOR revolve**; a NEW correlation branch `_correlate_revolve_faces` maps cylinders by radius (`outer_wall`/`inner_wall`) + caps by axis coordinate (`cap_lo`/`cap_hi`), fail-loud on a gap. Three Codex1 blockers absorbed: B1 the derived radial mode (tube/solid) is topology skeleton (shared `revolve_radial_mode` feeds evaluator+signature+correlation; radii are values); B2 v1 profile = exactly one rectangle (handler+evaluator); B3 extrude XOR revolve enforced symmetrically + in evaluator. Two-round; Codex2 signoff. KB grows (revolve page + golden recipe + crossing-axis trace; manifest 0.3.0→0.4.0). `aiadra-mechanical` adapter_schema_version **0.1.4 → 0.1.5**; aiadra-mechanical 127 + aiadra-core 406 green; NO aiadra-core/bundle/schema/Glossary change. The taxonomy now has five features (extrude/revolve creation; fillet/hole/chamfer referencing). Prior: arc 20260622-3 (**Chamfer BUILT — the fillet's edge-reference twin** (ADR/0037 D8; no ADR change): `mechanical.add_chamfer_feature` bevels one sharp edge with a flat symmetric `distance_mm` cut, reusing the ADR/0038 edge-reference machinery. `build_fillet_payload`→shared `build_edge_reference_payload(operation_kind=…)` + thin wrappers (Codex1 Q1). The bevel is a PLANE, claimed by construction as `face:chamfer` — the first PLANAR exercise of ADR/0038 A3's mandatory produced-face claim (neutered claim fails loud, no silent base role; Codex1 R1). `_apply_one_chamfer` in the fold (dispatch fillet|chamfer|hole); signature treats chamfer like fillet (anchor skeleton, `distance_mm` value per A2); oversize distance = Class-2. Two-round convergence; Codex2 signoff. KB grows (chamfer page + golden recipe + negative trace; manifest 0.2.0→0.3.0). `aiadra-mechanical` adapter_schema_version **0.1.3 → 0.1.4**; aiadra-mechanical 110 + aiadra-core 406 green; NO aiadra-core/bundle/schema/Glossary change. The spine is now exercised at N=3 (fillet edge / hole face / chamfer edge-twin) across cylindrical + planar produced faces. Prior: arc 20260622-2 (**Hole-as-feature BUILT — first FACE reference; [ADR/0038](Docs/ADR/0038-persistent-feature-reference-identity.md) amended (A1–A3)** (ADR/0037 D8): `mechanical.add_hole_feature` cuts one circular through-hole on a simple cap face; the committed `target_face` is an engine-owned recipe-anchored reference (face role + parent-prefix signature) — the display `face_id` is input vocabulary only. ADR/0038 amendment: A1 `target_face` shape (cap-only is an operation guard); A2 produced-feature value params (`diameter_mm`/`center_*_mm`, fillet `radius_mm`) excluded from `topology_signature` (move/resize within a face = parameter edit, retargeting = skeleton); A3 the produced-face claim is mandatory (`BlendHint`→`ProducedFaceHint`; zero/not-found fail loud; deterministic `#k`; by-construction source generalized `MakeFillet.Generated`→`Cut.Modified`). Sketch-XY placement (stable under a width edit); v1 simple-cap + fit-within-face enforced on every path — handler AND evaluator fold (Codex2 B1: a later parameter-edit breach fails Class-1 before the kernel). Three-round convergence; Codex3 signoff. KB grows (hole page + golden recipe + negative trace; manifest 0.2.0). `aiadra-mechanical` adapter_schema_version **0.1.2 → 0.1.3**; aiadra-mechanical 98 + aiadra-core 406 green; NO aiadra-core/bundle/schema/Glossary change. The ADR/0038 spine is now validated at N=2 on a new (face) reference shape. Prior: arc 20260621-2 (**Fillet/round feature BUILT — first modeling-paradigm feature; [ADR/0038](Docs/ADR/0038-persistent-feature-reference-identity.md)** (ADR/0037 D8 step 1): `mechanical.add_fillet_feature` rounds a single sharp edge; the committed `target_edge` is an engine-owned recipe-anchored reference (sorted `adjacent_face_roles` + `edge_kind` + parent-prefix `resolved_against_topology_signature`) — the display `edge_id` is input vocabulary only (ADR/0038, the write-side sibling to ADR/0035). `evaluate_part` is now a sequential fold with by-construction blend provenance; parameter-edit survives / topology-change + missing-role + ambiguity + removed-parent fail loud; blend role `…:face:blend` + tangent edges via existing lanes. Codex2 B1 (non-sharp rejected Class-1 before staging) + B2 (no `…/None:face:hole_wall` placeholder) absorbed; Codex3 signoff. KB strand opened (`Docs/KB/` manifest + fillet page + golden recipe + negative/repair trace, each `source_context`-attested). `aiadra-mechanical` adapter_schema_version **0.1.1 → 0.1.2**; aiadra-mechanical 79 + aiadra-core 406 green; NO aiadra-core/bundle/schema/Glossary change. Prior: arc 20260619-2 (**Command model + toolbar BUILT — slice 6b of ADR/0033 D10 step 6** (no new ADR — pure Studio consumer realizing D9): a data-close command taxonomy (`src/commands/`) — the single source of truth for the compact toolbar, the right-click menu, and a focus-guarded keyboard dispatcher; **Codex1 B1** split scene facts (`hasCanonicalPart`/`hasReferenceGeometry`/`hasRenderableScene`) so the imported-only lane keeps fit/reset+modes (grid always on; canonical-only reserved); live mode/grid lifted out of the Viewport into a `useSyncExternalStore` store (Codex1 N1; toolbar/menu/keyboard write, Viewport applies+reports scene facts); inline-SVG icons (no dep); `operations` group a reserved disabled placeholder. aiadra-studio **122 vitest** (107→122) + tsc + build; aiadra-core/mechanical untouched; NO contract/schema/bundle change. Next: 6c interaction shell (selection filters / hover / nav cube / standard views). Prior: arc 20260619-1 (**Appearance + settings BUILT — slice 6a of ADR/0033 D10 step 6** (decomposed 6a/6b/6c; no new ADR — pure Studio consumer realizing D8): a typed settings registry (validate-loud, subscribe, hydrate + unknown-key quarantine, fail-loud-on-newer-version) persisted under Electron `userData` via a NEW `loadSettings`/`saveSettings` IPC where **main owns path AND shape** (pure shared validator; load-tolerant/save-strict-version — Codex1 B1 + Codex2 R2); a coordinated theme owning the FULL live display palette (Codex1 B2) with live `Viewport.applyTheme`, `paperBody` inheriting background, light-green `#E4EFDF` default; behavior settings (settleMs/defaultMode/gridDefault); a minimal Appearance sidebar panel. aiadra-studio **107 vitest** (74→107) + tsc + build; aiadra-core/mechanical untouched; NO contract/schema/bundle change. Next: 6b command/toolbar, 6c interaction shell. Prior: arc 20260610-1 (**Display modes BUILT — canonical lane LIVE; stopgap RETIRED** (ADR/0033 D10 step 5; realizes D7; no new ADR — pure Studio consumer arc on the v1.1 payload): the five Creo modes (Wireframe / Hidden Line / No Hidden / Shading / Shading-With-Edges) live GPU approximation on true model edges + exact HLR overlay at camera-settle; viewport now orthographic/Z-up; `modes.ts` D7 matrix + `faceStyle: shaded|paper|none` opaque-body axis; `settle.ts`/`overlay.ts`/`displaySource.ts` + DEV-only fixture lane (production-excluded via `assert-no-fixtures.mjs`); `pickTargets` raycast firewall; **`list_parts` bridge method** over Tier-1 `protocol.query(kind="Part")` (Codex1 B1); Hidden Line live dim pass `depthTest:false` (Codex1 B2). Codex2 signoff no blockers. aiadra-studio **74 vitest** + tsc + build; aiadra-core/mechanical untouched; NO contract/schema/bundle/Glossary change. Carry-forward: desktop-acceptance run, benchmark packet (D1), 3 pre-existing lint problems. Unblocks step 6 (Appearance + interaction shell). Prior: arc 20260609-2 (**View-dependent HLR BUILT — [ADR/0036](Docs/ADR/0036-view-dependent-hlr-contract-v1-1.md)**: Display contract v1.0→**v1.1** additive — the reserved `view_dependent` slot populated (ADR/0033 D10 step 4); `ViewDependentPayload` with 6-field `identity_echo` + classified visible/hidden segments + outlines under an ephemeral-identity firewall; contract-complete 2D view frame; separate `display_hlr` read primitive (camera-settle cadence, never per-frame); ONE topology-identity source (shared `topology.py` layer — Codex1 B1 hard stop); pinned 5-step correlation disambiguation; counted sliver policy; HLR cache keyed on full identity (Codex2 B1). `aiadra-core` 0.12.0→**0.13.0**; `adapter_schema_version` stays 0.1.1; NO bundle/schema/Glossary change. aiadra-mechanical 63 · aiadra-core 406 · aiadra-studio 49 vitest + tsc + build green. Unblocks display modes (step 5). Arc 20260609-3 closed in the same window: **[ADR/0037](Docs/ADR/0037-modeling-paradigm-benchmark-and-knowledge-architecture.md) ACCEPTED** — modeling-paradigm benchmark (paradigm-not-product hard lines) + AI-authoring knowledge architecture (original KB + manifest, RAG-first/fine-tune-later with four falsifier gates, `source_context` trace provenance, five-item per-feature acceptance bundle); PTC Creo docs fenced as research-material-only (git-ignored) with the D3 operational firewall; closes the ADR/0026 §10 deferred KB item. Prior: arc 20260609-1 (**Rendering & topology foundation BUILT — [ADR/0035](Docs/ADR/0035-display-representation-contract-and-topology-identity.md)** — the first *real* Display build (ADR/0033 D10 step 3; gate cleared by ADR/0034). The versioned read-only **Display Representation contract v1** (kernel-neutral DTO: identity / face-grouped triangles + true normals / true edges by id+kind / selection payload / **reserved `view_dependent` HLR slot** / cache+invalidation / counters; mirrored TS type) + **engine-owned feature-anchored topology identity** (`<feature>[/<primitive>]:<kind>:<role>`; engine-minted `skp_` primitive anchors; recipe-first/geometry-second correlation; **fail loud on missing/malformed `skp_` ids — no placeholders**, Codex2 B1) + **deterministic `topology_signature`** (value-independent; stable across parameter edits — NOT a counter) + a **read-only Native Engine operation category** (separate `add_read_operation` lane + non-staging `NativeEngineReadContext` + parallel dispatch with no draft/no audit + `display_representation` primitive with fail-loud engine resolution) + engine-side OCCT generation (`aiadra-mechanical/display.py`: BRepMesh tessellation grouped by face + true edges + sharp/tangent/seam classifier; OCCT stays in the engine, core kernel-neutral). **Crux PROVEN**: display ids identical across a depth edit/recompute with `topology_signature` stable + real geometry move + role-to-shape correspondence; square-prism symmetric trap holds; tangent classifier proven on a real OCCT fillet fixture; pick-path smoke recovers `face_id` via a three.js raycast. Two-round Codex convergence (Codex1 B1/B2 pre-build; Codex2 B1 placeholder-fix + B2 minor bump at close). **`aiadra-core` 0.11.2 → 0.12.0** (additive public Tier-1 surface), **`aiadra-mechanical` `adapter_schema_version` 0.1.1** (engine-internal); **NO bundle/schema/Glossary/Manifesto change** (display data is not Product Truth). aiadra-core 375 tests · aiadra-mechanical 47 · aiadra-studio 43 vitest + tsc + build green. Realizes ADR/0033 D2/D3/D4/D5/D7/D10; merges ADR/0032 D8. Prior: arc 20260603-4 — **Licensing & third-party-kernel compliance DECIDED — [ADR/0034](Docs/ADR/0034-licensing-and-third-party-kernel-compliance.md)** (strategic decision; legal/repo/CI implementation = a defined follow-up arc; NOT final legal advice + 5-item attorney list; informed by a verified deep-research pass). **`AGPL-3.0-only` uniform** (§13 anti-modified-SaaS lever; per-component split rejected) + **non-exclusive license-back CLA** (Qt-style; preserves dual-license/commercial-sale/acquisition; assignment fallback; no external contributions until entity+CLA+process exist) + **SPDX/category graph-validated dependency policy** (Codex1 B1 — literal "MIT/BSD/Apache/LGPL only" missed `ISC`/`BlueOak`/`CC-BY`/`(MIT OR CC0)` already in the tree → classes×buckets + npm+Python scan + SBOM + CI; GPL/AGPL denied) + **artifact-level OCCT/LGPL compliance** (Codex1 B2 — per distribution form; **static-WASM `occt-import-js` attorney-gated**; Python OCCT libs user-replaceable in shipped form). OCCT=LGPL-2.1-only+exception; `occt-import-js`=LGPL-2.1; OCP=Apache-2.0+LGPL-OCCT; permissive+LGPL-only preserves proprietary relicense (OCCT doesn't block the buyout); AGPL §13 unmodified-SaaS gap → commercial-license lever is the real protection. **Clears the Display rendering-foundation gate** (foundation canonical lane = engine-side dynamic-OCCT; static-WASM attorney = release prerequisite not a build blocker). Two-round arc (Codex1 B1/B2 + N1–N4 absorbed; AGPL pinned `-only`). No schema/bundle/aiadra-core/Glossary change. Prior: arc 20260603-3 (**AIADRA Studio Display & UX strand OPENED — vision/scope [ADR/0033](Docs/ADR/0033-studio-display-ux-vision.md), Creo-10-benchmarked**. UX named existential by Petre; the screen-space viewport stopgap (`baf52d2`) hit its ceiling (dashed edges, missing surface-vs-surface silhouettes, mode taxonomy) and is end-of-lifed as the foundation (regression baseline only). Crux: Creo-grade display/hidden-line/silhouette/selection all need a **BREP-derived Display Representation contract**, not screen-space → rendering foundation = topology foundation = selection foundation (**milestone 2 merges in**). ADR/0033 (11 decisions): Creo-10 benchmark + non-clone guardrail (D1); BREP-derived true topology (D2); versioned read-only **Display Representation contract** as the central deliverable — identity/render/selection/HLR payloads + cache/invalidation + narrow-bridge boundary (D3, Codex1 B1); **engine-side OCCT** for canonical geometry + renderer WASM (`occt-import-js`) for reference imports, `opencascade.js` spike-only (D4, Q1=(c)+hybrid); **engine-owned topology identity** with lifetime/invalidation, NEVER raw OCCT subshape handles / `occt-import-js` `brep_faces` ranges, imports get ephemeral IDs until ingested, identity tested across an edit (D5, Codex1 B2); **OCCT HLR** view-dependent cached-on-settle, exact+poly (D6); Creo display-modes taxonomy (Wireframe/Hidden-Line-dimmed/No-Hidden/Shading/Shading-With-Edges; reflections deferred) (D7); typed **settings registry** under OS user-data, versioned JSON, NOT Workspace (D8); **command model** → compact toolbar now / ribbon later (D9); strand roadmap (vision → benchmark packet → foundation spike → HLR spike → modes → appearance+shell) + boundary (see/select here, model-changing later) + perf budgets (D10); **gated on the Licensing ADR** (D11). Two-round arc (Codex1 direction accept + B1/B2 absorbed). No schema/bundle/aiadra-core/Glossary change — UI vision arc. **Parallel (not an arc yet): a licensing deep-research pass in flight** — working direction **AGPLv3 + CLA + permissive/LGPL-only dependency policy** (OCCT is LGPL-2.1 → fine for proprietary relicensing; the CLA preserves dual-license/buyout optionality) → its own Licensing & third-party-kernel-compliance ADR → Codex, gates the foundation arc. Prior: arc 20260603-2 (**AIADRA Studio STEP import ENABLED via read-only `app://` origin + built-Electron smoke — external import lane now complete for BOTH formats (STL + STEP)**. Route (b): the BUILT renderer is served from a read-only, path-confined `app://bundle` origin (not `file://`) so occt-import-js fetches its WASM via its normal `locateFile` path — no `wasmBinary` plumbing, no sandbox/CSP weakening. B1 (handler security envelope — privileged scheme pre-ready, no `bypassCSP`; GET/HEAD only; confined under `out/renderer` via `electron/appProtocol.ts` pure helper + realpath/symlink guard; explicit MIME `.wasm`=`application/wasm`) + B2 (built-Electron smoke `scripts/step-electron-smoke.mjs` proves origin `app://bundle` + handler-served WASM + real controller/worker/occt path + non-zero geometry + zero `aiadra:*` IPC calls → STEP flipped on only because it PASSES) + Codex2 N1 (strict raw-URL `%2e` traversal reject) absorbed; 39 vitest + `tsc -b` + build + smoke clean; UI code only — no schema/bundle/aiadra-core version bump; next = milestone 2 (topological selection + the AIADRA Workspace lane). Prior: arc 20260603-1 **milestone-1b external STL import lane** — off-thread Web Worker parse → reference-only node; B1 untrusted-import gate + Codex2 B3 remove-during-loading race fixed; STEP deferred then resolved in 20260603-2. Prior: arc 20260602-6 **AIADRA Studio v1 milestone-1 bridge half BUILT + converged** — secure Electron shell + Studio-owned Python NDJSON JSON-RPC bridge over Tier-1 protocol; Codex2 signoff; N1/N2/N3 hardening nits absorbed. Prior: arc 20260602-5 AIADRA Studio scope [ADR/0032](Docs/ADR/0032-aiadra-studio-scope.md) landed — standalone Electron desktop app = primary Workspace Browser; VSCode secondary; `aiadra-mechanical` v0.0.2 engine parked) — curated navigation / cache layer; fifteen active patterns; **eleven** Coherence Checklist items (+ "Native engine boundary" earned arc 20260531-11 via Codex1 B1 near-miss; passed + tightened in arcs 20260531-12 + 20260531-13 + 20260601-1 + 20260601-2 + 20260601-3 + 20260601-4 + 20260601-5 — boundary held under REAL distribution at install/discovery/dispatch; arc-5 polish locks `event_log_last_event_id()` proposed-workspace semantics + fail-loud on corruption); **Spec → code transition COMPLETED; Wedge-001 + Wedge-002 implemented; ADR/0025 production-grade runtime scope COMPLETE; ADR/0026 Ring 2 scope landed; Phase A + Phase B + Phase C + Phase D Ring 2 implementations landed; Ring 2 implementation strand COMPLETE (9-of-9 ADR/0026 §2 contracts); ADR/0027 AIAD positioning + Native Engine posture landed; ADR/0028 Ring 3 Native Engine Implementation contract landed; ADR/0029 Part authoring SCN landed; aiadra-core 0.10.0 → 0.11.0 Native Engine API surface landed; ADR/0030 Wedge-003 scope ADR landed; Wedge-003 implementation arc landed; Wedge-003 friction-log review arc landed (first analytical-only arc in AIADRA history); polish arc landed (FIRST PATCH version bump in aiadra-core history — `aiadra-core 0.11.0 → 0.11.1` for user-visible edge behavior change on `event_log_last_event_id()` corrupt-event-log fail-loud semantics; 5 conformance tests for the existing helper + NEW `aiadra-core/tests/integration/README.md` test-hygiene rule; first arc with Codex review-round skipped under Petre signoff for resource-exhaustion; 383/383 tests passing).** at [`aiadra-core/`](aiadra-core/) v0.9.0 + bundle v0.27.0 + 258 of 258 tests passing. ADR/0027 (arc 20260531-11) pins AIADRA as an **AI-Augmented Design** platform (distinct from CAD as a system category) implementing AIADRA-native authoring engines per domain using third-party kernels as **libraries** (OCCT for mechanical; KiCad libs for electrical); **does NOT wrap third-party applications** (FreeCAD-the-app etc. are research material only); Native Engines ship as ecosystem packages outside `aiadra-core` per D11. Multi-document amendments co-landed: Manifesto v0.4 + ArchitectureOverview v0.2 + Glossary v0.25; OQ-0004 + OQ-0005 superseded; OQ-0006 reframed; D13 uniform retrofit clause covers 29 downstream ADR + TruthModelSchema references via cross-reference (no individual ADR edits); D18 ADR/0005 §9 adapter shell preserved verbatim (wire names survive; semantic role reframed as Native Engine / Data Adapter dispatch contract); D17 Wedge-003 GATED on Part authoring SCN (working ADR/0029; new `part_changed` event with feature/geometry/vault deltas); no schema bundle bump (D18 zero-bytes-change); no aiadra-core version bump (positioning-only arc). NEW Coherence Checklist item **"Native engine boundary"** at SystemState §3. Sequencing for the post-Ring-2 era: (a) Ring 3 scope ADR/0028 → (b) Part authoring SCN ADR/0029 → (c) Wedge-003 scope ADR → (d) `aiadra-mechanical` ecosystem-package implementation; per-engine research arcs in parallel. Phase D (arc 20260531-10) lands `protocol.simulate(draft) → ValidationReport` (collects structured FAIL outcomes without raising per Codex1 B1; no audit; asserts open per Codex1 B3 lifecycle) + `protocol.explain(workspace, ref, *, depth=1) → ExplanationTree` (workspace-ref traversal accepting Number/UUID/`<obj>:relationship:<rel_id>` per ADR/0015; per Codex2 B2 R3: depth>0 walks BOTH outgoing AND incoming via `list_working_sidecar_uuids()` scan; fails loudly like `query`) + `protocol.explain_failure(failure, *, bundle_version, depth=0) → ExplanationTree` (per Codex1 B2: accepts ValidationOutcome / ExplanationNode / dict satisfying ADR/0026's `explain(failure, *, depth)` contract). NEW `aiadra_core/explain/` module with frozen recursive `ExplanationNode` + 4 typed factories per Codex1 B4 enforcing canonical per-kind detail keys (object/relationship/event/validation_error) + `classify_exception()` helper. NEW `aiadra_core/audit/` module per ADR/0026 §9 — `AuditRecord` with 7 verbatim fields + single-line JSON at `.aiadra/audit/YYYY-MM-DD/tx_NNNN-failed-<short-hash>.jsonl`; `AuditConfig` defaults (max_entries_per_agent=100, max_age_days=30, max_total_mb=50); `compute_prune_set` enforces ALL THREE retention dimensions per Codex2 B1 R3 (per-agent bucket grouping via JSONL parse; `<unknown>` bucket for null/missing; per-agent buckets independent). TransactionDraft `_audit_emitted` flag + `_emit_audit_once()` + `audit_failure()` method per Codex1 B3 single-record semantics — three emission paths (audit_failure / rollback / commit-on-CommitError) all funnel through; per Codex2 N1 R3 `rollback` raises ValueError on invalid `reason_classification` (was silent coerce). `validate()` refactored to delegate to `_validate_internal(*, collect_failures)` — commit path raises on first fail; simulate path collects all; per Codex2 B3 R3 SKIPS dependent fold + b6 checks when schema_failures > 0 (with explanatory SKIPPED outcome) + broad-catch safety net in collect mode. ValidationOutcome extended with optional `tree: ExplanationNode | None` field (backward-compat). NEW dirty-guard carve-out for `.aiadra/audit/` PREFIX (audit-config.yaml at `.aiadra/` ROOT remains guarded). NEW 4 CLI commands: `aiadra explain` (human / `--json`) / `aiadra audit list` / `aiadra audit show` / `aiadra audit-prune --dry-run`. NO `aiadra simulate` CLI (defer per Phase B query / Phase C propose precedent). Bundle v0.27.0 pin-only; new MigrationStep chain entry. Phase A stub-guard test INVERTED — now asserts ALL 9-of-9 contracts EXIST. 60 new Phase D tests (51 R2 + 9 R3 Codex2 regressions). **Three-round arc** (Codex3 signoff R3). **Ring 2 implementation strand COMPLETE** — 9-of-9 §2 contracts (`inspect` / `query` / `propose` / `modify` / `simulate` / `validate` / `explain` / `commit`+`rollback` (terminal family) / `release`) in place across Phases A-D shipped in one calendar day. Phase C (arc 20260531-9) lifts `TransactionDraft` from internal-only to first-class Ring 2 entity: new `protocol.propose(workspace, *, kind, params, actor="agent") → TransactionDraft` allocates a fresh draft across 17 propose kinds (init + 5 create_* + change_parameter + add_acceptance_criterion + 7 link_* + attach_file + release); `protocol.modify(draft, *, kind, params, actor="agent")` extends an existing draft across 15 modify kinds (init + release excluded per Codex1 B2 — neither composes with in-flight mutations); `propose_kinds()` + `modify_kinds()` introspection helpers return sorted tuples. **Codex1 B1 draft-aware reads**: 4 helpers in `transaction/operations.py` + all 5 mutation ops accept `existing_draft=` so a freshly-created Object can be mutated within the same Transaction; transaction_id inherited; commit_message_lines extended (not replaced). **Codex1 B3 lifecycle**: private `_lifecycle_state` on TransactionDraft + `_assert_open()`; second commit / commit-after-rollback / rollback-after-commit / modify-on-closed-draft all raise consistently. **Codex1 B4 provenance discipline**: `actor="agent"` cannot self-attest `new_fact_provenance.category="human_input"` per ADR/0026 §5; CLI passes `actor="human"`. **CLI delegation complete**: all state-changing commands route through `protocol.propose` via `_run_protocol_propose` helper. **Codex2 B1 (R3) proposed-state B6 scan**: `validation.binding.find_mutation_after_binding_violations` split into pure-stream variant; `TransactionDraft.validate()` runs `_validate_proposed_b6_scan()` over `committed_events + self.events` for every non-INIT draft — closes the composability hole where `modify()` could stage `link_executes` + `change_parameter` on the bound target and pass `draft.validate()` (per-op `_mutation_prohibition_hook` only walked disk state). 42 new Phase C tests (35 R2 + 7 R3) + 1 stub-guard test updated for new surface. **Two-round arc by Claude2/Claude3 split** (Codex3 signoff R3). Bundle v0.26.0 pin-only (byte-identical to v0.25.0 except _index.json+_digest.json); new MigrationStep chain entry. No new Pattern Catalogue row; no new Coherence Checklist item ("AIADRA Core hosts nothing" load-bearing — Python API + CLI refactor + binding scan; no service). Phase B (arc 20260531-8) lands `query(workspace, *, kind, filter, locality, staleness) → list[ObjectView]` covering cumulative release graph + working set per Codex1 B1 (ObjectView extended with source/revision_id/release_label defaulted fields preserving Phase A backward-compat); locality/staleness matrix per ADR/0001 §6 + ADR/0026 §4 (remote_only/must_sync always fetch; fresh_within_<N><unit> fetches if FETCH_HEAD missing/stale; local_if_fetched does "one fetch otherwise" per ADR/0001); `_run_git_fetch` bounded 30s timeout with subprocess errors all → `NetworkUnreachableError(ConnectionError)` per Codex1 B3; `FetchTooStaleError` dropped per Codex1 Q11; `_check_locality_staleness` split into `_validate`/`_enforce`; `_object_view_from_sidecar` helper per Codex1 N2; predicate-rebind avoids shadowing builtin per N1; deterministic ordering per N3. **Codex2 B1 fail-loud absorption (R3)**: removed all silent skip-on-invalid catches; corrupt working sidecars / Release Manifests / released Revisions propagate raise; defensive `continue` on missing manifest revision fields → explicit `SchemaValidationError`. `query` is AI read primitive over Product Truth NOT best-effort search index — agents need to know when substrate is corrupt; aligned with Manifesto P5 + ADR/0002 reject-loudly. **Three-round arc** (matches arc 20260531-6's cadence — both where API contract tightened across rounds). 35 new Phase B tests + 2 net Phase A test splits → 156/156. No new Pattern Catalogue row; no new Coherence Checklist item ("AIADRA Core hosts nothing" load-bearing — git fetch hits project's own origin remote; agent-initiated network only). Phase A (arc 20260531-7) lands `aiadra_core/protocol/__init__.py` (~360 LOC) as canonical Ring 2 agent-facing surface: 5 operations (inspect / validate / commit / rollback / release) + 6 type shapes (ObjectView frozen+sidecar deep-copy; ValidationReport frozen+tuple outcomes; RollbackResult frozen; CommitResult + ValidationOutcome + TransactionDraft re-exported) + 2 new exceptions (ObjectNotFoundError, ProjectPinError wrapping Phase 1 pin exceptions with `__cause__`). CLI delegation scope-limited per Codex1 B1 (cli/inspect.py 89→30 LOC; cli/validate.py 193→55 LOC with logic moved INTO protocol.validate per Codex1 N2; cmd_release + _run_draft commit through protocol.*; create/change/link/attach/add-criterion stay on transaction.operations.* until Phase C propose/modify). Locality/staleness API value classes distinguished per Codex1 B2 (invalid → ValueError; recognized non-default → NotImplementedError "Phase B"). New TransactionDraft.rollback(*, reason) clears 11 staged collections per Codex1 Q5+N5; Phase A discard-only (Phase D adds audit per ADR/0026 §9). Bundle v0.24.0 pin-only (byte-identical to v0.23.0 except _index.json+_digest.json); new MigrationStep chain entry. 26 new tests covering: module exports + negative future-op stubs guard; UUID-or-Number inspect; sidecar deep-copy independence; 5 locality/staleness value-class tests; pin-error wrapping with __cause__; sentinel commit-wrapper; rollback-clears-all-collections; CLI thin-wrapper unchanged-behavior; chain v0.19.0→v0.24.0 migration. **Two-round arc** (Codex2 signoff with one-line release_label default polish). No new Pattern Catalogue row; no new Coherence Checklist item (existing "AIADRA Core hosts nothing" load-bearing — Phase A is Python API + light CLI refactor; BYO-AI posture preserved per ADR/0026 §0). Ring 2 scope ADR (arc 20260531-6, [ADR/0026](Docs/ADR/0026-ai-action-protocol-scope.md)) — **first forward-look ADR after ADR/0025's production-arc strand closed**; scope-first ADR (no code in this arc) per ADR/0023/24/25 precedent; pins agent-facing 9-operation contract surface (inspect / query / propose / modify / simulate / validate / explain / commit-rollback / release) + 5-phase implementation roadmap (Phases A-D core + optional Phase E ecosystem RPC package OUTSIDE core per Manifesto P11) + **§0 BYO-AI posture** (AIADRA Core ships zero AI model code; Tier-1 Python + Tier-2 CLI vendor-neutral; cloud LLMs / local LLMs / deterministic scripts all call same contracts) + **mutation granularity intentionally OPEN** in Decision §2 (preserves future Ring 3 kernel-level operations + future CAD-model DSL per Petre's framing) + Phase 1 Draft-then-commit formalized as Layer 3 Transaction contract (Codex2 N1: only `commit` writes; `simulate`+`validate` are no-write functionally-identical operations differing in lifecycle position) + locality+staleness as Phase A defaulted no-op kwargs (Phase B implements non-default) + provenance enum reconciled to v0.23.0 implemented `{human_input, ai_proposal, computed_result, measured}` + transport tiering (Tier-1 Python + Tier-2 CLI in core, Tier-3 RPC like `aiadra-mcp` as SEPARATE ecosystem package) + no hosted service / no live coordination / multi-agent contention deferred + **OQ-0003 resolved** (bounded audit log at `.aiadra/audit/YYYY-MM-DD/tx_NNNN-failed-*.jsonl`; `audit-config.yaml` at `.aiadra/` ROOT remains guarded; carve-out scoped to `.aiadra/audit/` subdirectory prefix only per Codex2 B1; diagnostic-only NOT truth; default git-tracked) + **Knowledge Base / RAG explicitly DEFERRED** to future scope ADR (no `knowledge:kb_*` URI reservation today since `fact_provenance.derived_from[]` is already unconstrained array of string). **Three-round arc** (R1 design + Codex1 B1+B2+N1+N2+brainstorm; R2 ADR draft + Codex2 B1 path contradiction + N1 lifecycle wording; R3 patches + Codex3 signoff). **First arc with explicit user-led discussion turn** (`Claude1_brainstorm.md` per Petre BYO-AI / RAG / kernel-level questions before Codex review). §6 prepends arc 20260531-6 entry. No schema bundle bump (meta-decision; bundle stays v0.23.0; aiadra stays 0.5.0). No new Pattern Catalogue row; no new Coherence Checklist item ("AIADRA Core hosts nothing" is load-bearing for this ADR). Object Type catalogue still 9, named relationship-type catalogue still 14. **Next likely arc**: OPEN — Ring 2 strand complete; no pre-pinned sequencing remains. Plausible directions: (a) **Wedge-003 scope ADR** (third spike informed by full v0.27.0 runtime + complete Ring 2 surface); (b) **Ring 3 Domain Adapter scope ADR** (parallel forward-look to ADR/0026; Petre's "AI shouldn't push CAD buttons" framing); (c) `derived_geometry_from` (blocked on b); (d) FreeCAD adapter implementation (blocked on b); (e) **Knowledge Base / Ring 6 scope ADR** (sibling to ADR/0026 per Decision §10 item 1); (f) **ecosystem RPC package** (`aiadra-mcp` or similar Tier-3 transport per ADR/0026 §6; lives OUTSIDE `aiadra-core` per Manifesto P11); (g) housekeeping bundle (migrator integration test v0.19.0→v0.20.0 gap + stale-digest CLI polish + SystemState Pattern Catalogue split-out).

## Planning framework (rings)

Planning happens in concentric rings around the Product Truth Model. See [Docs/ArchitectureOverview.md](Docs/ArchitectureOverview.md) for the current five-layer model (Truth Model → Validation → AI Action Protocol → Project Control → Native Engines + Data Adapters, per [ADR/0027](Docs/ADR/0027-aiad-positioning-and-native-engine-posture.md)) and how the Ring 0 ADRs realize it. [Docs/Discussions/20260517/Claude1.md](Docs/Discussions/20260517/Claude1.md) §3 holds the original framework, refined by the Overview. Current ring is recorded in the latest snapshot.
