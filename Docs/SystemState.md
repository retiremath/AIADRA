# SystemState

> Curated navigation / cache layer for AIADRA's working method.
> **Not an authority layer.** Decisions live in ADRs; this document points to them and asks the right questions at arc time.
> Authority hierarchy: ADRs > Manifesto > TruthModelSchema / ArchitectureOverview > SystemState.
> Anti-goals: second TruthModelSchema; decisions not backed by ADRs; >140 lines; narrative recap; ceremonial-but-unread.
> Last updated: arc 20260531-1 (Phase 0 skeleton — `aiadra-core/` package + canonical infrastructure) at 2026-05-31.

## 1. Current Front

**Ring 1 catalogue work — framework complete + seed Object Type catalogue at nine + named relationship-type catalogue operationally complete except `derived_geometry_from`. V&V framework operationally executable end-to-end with execution-instance atomic binding.**

- Spine + Promotion Rule + amended commitment 6 + seed catalogue (Part / Requirement / Assembly) + cross-project framework (ADR/0008) + Number allocation (ADR/0004) all pinned.
- **Object Type catalogue (9):** [Part](ADR/0005-object-type-part.md), [Requirement](ADR/0006-object-type-requirement.md), [Assembly](ADR/0007-object-type-assembly.md), [Component](ADR/0014-object-type-component.md) (first External pointer Object), [SoftwareModule](ADR/0016-object-type-software-module.md) (second External pointer Object + first dual-Promotion-path Type), [Drawing](ADR/0017-object-type-drawing.md) (first Attachment-bearing Object), [EvidenceArtifact](ADR/0019-object-type-evidence-artifact.md) (second Attachment-bearing Object), [TestProcedure](ADR/0020-object-type-test-procedure.md) (third Attachment-bearing Object), **[TestExecution](ADR/0022-test-execution-model.md) (fourth Attachment-bearing Object + first execution-instance Object Type)**. All three Tier-2 candidates from Glossary candidate pool promoted (Drawing / EvidenceArtifact / TestProcedure); TestExecution lands as the V&V framework's execution-instance close.
- **Relationship-type ADRs landed (14 types, 9 decisions):** [`satisfies`](ADR/0009-relationship-type-satisfies.md), [`composed_of`](ADR/0010-relationship-type-composed-of.md), [`mated_to`](ADR/0011-relationship-type-mated-to.md), [`derived_from` and `refines`](ADR/0012-relationship-types-derived-from-and-refines.md), [`allocates_to`](ADR/0013-relationship-type-allocates-to.md) (target Type union extended by ADR/0014 / 0016 / 0017 / 0020 to `Part | Assembly | Component | SoftwareModule | Drawing | TestProcedure`; EvidenceArtifact excluded per ADR/0019), [`parameter_expression`](ADR/0015-relationship-type-parameter-expression.md), [`depicts`](ADR/0018-relationship-type-depicts.md), [`verifies` and `tested_against` and `cites`](ADR/0021-relationship-types-v-and-v.md) (combined V&V ADR; closes V&V wiring end-to-end), **[`executes` and `executed_on` and `produces`](ADR/0022-test-execution-model.md) (combined test-execution-model ADR; first execution-instance relationship category — Fixed-only binding, status-sensitive cardinality)**. **Named relationship-type catalogue from [ADR/0009 §3](ADR/0009-relationship-type-satisfies.md) operationally complete except `derived_geometry_from`** (awaits FreeCAD Domain Adapter scope per [Manifesto P12](Manifesto.md)).
- Cycle classes operationally active: `trace_graph` (`satisfies`, `derived_from`, `refines`, `allocates_to`, `depicts`, `verifies`, `tested_against`, `cites`, `executes`, `executed_on`, `produces`), `acyclic_dependency` (`composed_of`, `parameter_expression`), `undirected_constraint_graph` (`mated_to`).
- **ADR/0022 precedents:** (a) **First execution-instance Object Type** — distinct from design-intent and Attachment-bearing canonical-record Types; records historical events rather than engineering intent; (b) **First execution-instance relationship category** — Fixed-only binding (Float schema-rejected); `endpoints[0].revision_id` unconditionally REQUIRED; cross-project opt-OUT default; (c) **First per-relationship-type-cardinality-at-release rules** with status-sensitive refinement (`executes` exactly 1; `executed_on` ≥1; `produces` ≥0 globally with `completed` SHOULD ≥1 diagnostic, `aborted` MAY 0, `inconclusive` MAY 0 or ≥1); (d) **First "resolution-by-declining-to-add"** — the ADR/0021 deferred Part / Assembly → EvidenceArtifact direct edge resolved by *not* adding a direct shape; multi-hop traversal `Part →tested_against→ TestProcedure ←executes← TestExecution →produces→ EvidenceArtifact` is the structural answer; (e) **Four partial supersessions** (ADR/0005 + ADR/0007 EvidenceArtifact-target half of `tested_against` resolved by declining-to-add; ADR/0019 `collection_context` load-bearing role; ADR/0020 event-payload sketch promoted to Object event taxonomy).
- Seven arcs end-to-end on the Claude↔Codex coordination protocol from arc 20260519-1; protocol continues to catch real Claude misses on round 1 (arc 20260520-1 Codex1: worked-example `derived_secondary` lineage violation + parameter `derived_from` syntax drift + three non-blockers including status-sensitive `produces` cardinality refinement).

**Spec → code transition completed; Wedge-001 + Wedge-002 both implemented; ADR/0025 production-grade `aiadra-core` runtime scope pinned; Phase 0 skeleton landed at [`aiadra-core/`](../aiadra-core/) (arc 20260531-1).** Wedge-001 implemented per [ADR/0023](ADR/0023-wedge-spike-scope-and-runtime.md) at [`spikes/wedge-001/`](../spikes/wedge-001/) (arc 20260530-1); Wedge-002 scope per [ADR/0024](ADR/0024-wedge-002-spike-scope.md) (arc 20260530-2); Wedge-002 implementation at [`spikes/wedge-002/`](../spikes/wedge-002/) (arc 20260530-3); production-grade runtime direction per [ADR/0025](ADR/0025-aiadra-core-runtime-scope.md) (arc 20260530-4). **Phase 0 skeleton arc 20260531-1 lands the production-grade `aiadra-core/` Python package** — library + thin CLI; 6 truth_model + 4 validation + 2 vault + 1 transaction-stub + 3 CLI modules (1,363 LOC Python); 33 schema files (5 object + 7 relationship + 13 event with `_base.schema.json` + allOf+$ref + 5 reservation + 1 _shared + manifest + _index + _digest); 38 tests passing. Read-only `aiadra validate` + `aiadra inspect` commands wired end-to-end + verify `.aiadra/schemas.yaml` project pin before any artifact read. Both Wedge fixture projects port verbatim to `aiadra-core/tests/fixtures/wedge_{001,002}/` with project pins. Three-round Claude↔Codex arc (3+2+0 blockers): R1 missing-fixture-pin + wrong-inspect-target + one-way-fold; R2 inspect-pin-also + restore-`_base.schema.json` (via `referencing.Registry` migration). All friction-item absorptions (F1 / F2 / F3 / W1 / W2 / W3) remain deferred to Phases 1-4 per ADR/0025. [OQ-0007](OpenQuestions.md) stays resolved; no new OQs.

**Next likely arc:** Phase 1 **runtime-behavior arc** per [ADR/0025 §1](ADR/0025-aiadra-core-runtime-scope.md). Implements F3 atomicity (Draft-then-commit per state-changing CLI command per [ADR/0025 §6](ADR/0025-aiadra-core-runtime-scope.md)) + W1 staged-release runtime (per [ADR/0025 §7](ADR/0025-aiadra-core-runtime-scope.md)) + W2 attachment-via-Transaction runtime (per [ADR/0025 §8](ADR/0025-aiadra-core-runtime-scope.md)). Includes W1 SCN (manifest `prior_stage_manifest_ref` + `stage_number` + `final_stage` + `release_staged` event) + W2 SCN (`<type>_changed` event payload extension with `attachment_delta`). Bundle bumps v0.19.0 → v0.20.0. **Recommended** — direct continuation per ADR/0025 5-phase roadmap. Alternative arcs: **Wedge-003 scope ADR** (extends V&V chain with Component / SoftwareModule / Drawing); **`derived_geometry_from`** (still gated on FreeCAD Domain Adapter scope per [Manifesto P12](Manifesto.md)). Both remain lower priority than continuing ADR/0025's sequencing.

## 2. Active Pattern Catalogue

| Pattern | Declared by | Applies to | Watch-out |
|---|---|---|---|
| Source-anchored asymmetric binary serialization (implicit source + single serialized target) | [ADR/0009 §1](ADR/0009-relationship-type-satisfies.md), [ADR/0010 §1](ADR/0010-relationship-type-composed-of.md) | `satisfies`, `composed_of`, `derived_from`, `refines`, `allocates_to`, `depicts` | Legitimate break for symmetric / multi-endpoint relationships per [ADR/0011 §1](ADR/0011-relationship-type-mated-to.md); `depicts` extends this pattern with an `occurrence_context` sub-field on the target endpoint when documenting placed instances (necessitated by the Drawing source-anchor's lack of implicit Assembly visibility) |
| Undirected multi-endpoint serialization (all semantic endpoints serialized; owning Object is storage carrier) | [ADR/0011 §1](ADR/0011-relationship-type-mated-to.md) | `mated_to`; future undirected geometric | Owning Object is carrier, not semantic endpoint |
| Asymmetric multi-endpoint serialization (separate `output_endpoint` singleton + `input_endpoints` list with stable ids; first **fact-level endpoint addressing** — parameter addresses `<object_uuid>:parameter:<id>`; expression payload with explicit `variables[]` symbol-to-endpoint bijection) | [ADR/0015](ADR/0015-relationship-type-parameter-expression.md) | `parameter_expression` | Output endpoint MUST NOT carry `occurrence_ref` (no occurrence-local outputs in seed); `variables[]` bijection enforces canonical dependency set; cross-Object scope of `acyclic_dependency` closure |
| Direct-binding (relationship-level Float / Fixed; default Float; release materializes Fixed) | [ADR/0009 §4](ADR/0009-relationship-type-satisfies.md), [ADR/0010 §5](ADR/0010-relationship-type-composed-of.md) | `satisfies`, `composed_of`, `derived_from`, `refines`, `allocates_to`, `depicts` | Legitimate break for indirect-binding relationships per [ADR/0011 §5](ADR/0011-relationship-type-mated-to.md) |
| Indirect-binding (no relationship-level binding; delegated to address mechanism; endpoint `revision_id` as cross-check) | [ADR/0011 §5](ADR/0011-relationship-type-mated-to.md) | `mated_to`, `parameter_expression` | Endpoint `revision_id` is never authority; hard-fail on mismatch with occurrence-path resolution |
| Multi-endpoint stable ids (endpoints with n≥2 carry stable local id per S0 commitment 7) | [ADR/0011 §3](ADR/0011-relationship-type-mated-to.md) | `mated_to`; future multi-endpoint | Single-endpoint relationships don't need ids (positional addressing OK) |
| Engineering-structure direct-external-endpoint NO default | [ADR/0008 §4](ADR/0008-cross-project-object-identity.md) | `composed_of`, `mated_to`, `parameter_expression`; future structural | Catalog reuse routes through local Binding Objects |
| Trace-relationship direct-external-endpoint opt-in (with Float external semantics) | [ADR/0009 §3](ADR/0009-relationship-type-satisfies.md) | `satisfies`, `derived_from`, `refines`; future trace relationships likely | Float external resolves to current released Revision; never working sidecar |
| Requirement-to-Requirement trace relationships (`trace_graph` cycle class; partial supersession of [ADR/0006 §"Decision 12"](ADR/0006-object-type-requirement.md) `acyclic_dependency`) | [ADR/0012](ADR/0012-relationship-types-derived-from-and-refines.md) | `derived_from`, `refines` | Cycles are graph-class-valid but semantically suspicious; tooling may warn, schema does not hard-fail |
| External pointer Object pattern operationalized (local Binding Object with discriminator-specific upstream binding; release-time content-hash integrity; mirrored `published_ref` with `upstream_fact_ref` chain validation; algorithm-qualified hash convention pinned by ADR/0016) | [ADR/0014](ADR/0014-object-type-component.md), [ADR/0016](ADR/0016-object-type-software-module.md) | Component, SoftwareModule (future MaterialSpec-like, etc.) | Local Binding Object owns adoption lifecycle. AIADRA Core hosts no registry / resolution service per [Manifesto P11](Manifesto.md). Released mates targeting Component-owned published_refs hard-fail if the upstream chain (pinned Revision → upstream pr id) doesn't resolve. SoftwareModule additionally invokes a *direct Promotion path* (basic capability test) for `workspace_git` first-party modules; the Binding-lifecycle path applies only when the discriminator is one of the four external-source values. |
| Attachment-bearing Object pattern operationalized (canonical bytes attached via new `attachment:` namespace; D7-escape made structural through required `source_authoring` role at release; derived roles carry `derived_from_attachment_id` lineage terminating at source; algorithm-qualified `content_hash` is authority, `vault_path` is non-authoritative locator hint; per-Type specialization of `source_authoring` semantic is the established template pattern) | [ADR/0017](ADR/0017-object-type-drawing.md), [ADR/0019](ADR/0019-object-type-evidence-artifact.md), [ADR/0020](ADR/0020-object-type-test-procedure.md) | Drawing, EvidenceArtifact, TestProcedure, TestExecution (future annotated-simulation candidates, etc.) | Every released Attachment-bearing Object MUST carry at least one `source_authoring` attachment with resolved `content_hash`. Derived attachments (rendered_primary / derived_secondary) MUST point back via `derived_from_attachment_id`; lineage chain terminates at `source_authoring`; cycles / dangling references hard-fail. Hash-against-retrieved-bytes mismatch is hard-fail at fetch / release. **Per-Type specialization of `source_authoring` semantic:** Drawing = authored canonical drawing payload; EvidenceArtifact = canonical evidence payload (raw result / signed report / measurement log; setup-only forbidden) AND every `parameter:` record carries `fact_provenance.derived_from` lineage to supporting `attachment:` records (full chain: parameter → derived_from → attachment → content_hash → Vault bytes); TestProcedure = canonical procedure document (signed test plan / SOP / test script / runbook / regulatory protocol; metadata-only stub forbidden) — TestProcedure `parameter:` records are nominal design facts, no `derived_from` lineage discipline; TestExecution = canonical record of the execution event (raw instrument output / signed inspector report / automated test framework output / video log; metadata-only stub forbidden) AND every `parameter:` record carries `fact_provenance.derived_from` lineage to supporting `attachment:` records (parallel to EvidenceArtifact — TestExecution parameters are observed measurements, not nominal design facts). AIADRA Core hosts no Vault, no renderer, no central registry per [Manifesto P11](Manifesto.md). |
| Deliverable-vs-emergent governing principle for `allocates_to` target inclusion (with wrong-verb authoring guardrail discipline within "MAY") | [ADR/0017 §"Decision §5"](ADR/0017-object-type-drawing.md), [ADR/0019 §"Alternatives §C"](ADR/0019-object-type-evidence-artifact.md), [ADR/0020 §"Decision §5"](ADR/0020-object-type-test-procedure.md) | `allocates_to` target Type union: `Part \| Assembly \| Component \| SoftwareModule \| Drawing \| TestProcedure` (EvidenceArtifact excluded) | Committed-to-produce deliverables MAY be `allocates_to` targets (Part / Assembly / Component / SoftwareModule / Drawing / TestProcedure). Emergent records MAY NOT (EvidenceArtifact — emerges from execution; right verb is `cites` per [ADR/0021](ADR/0021-relationship-types-v-and-v.md)). Within "MAY," per-Type wrong-verb authoring guardrails further constrain *which* Requirements can target the deliverable. TestProcedure-specific: only procedure-deliverable Requirements ("project shall publish a documented procedure") may target a TestProcedure; product Requirements awaiting verification ("bracket shall withstand 5400 N") target the `verifies` edge per [ADR/0021 §2](ADR/0021-relationship-types-v-and-v.md) instead. Schema cannot semantically distinguish; tooling MAY surface as non-blocking diagnostic. |
| Criterion-level Requirement addressing (optional `endpoints[].fact_ref` on target Requirement endpoint; optional `source_fact_ref` at relationship-record level for Requirement-source relationships; resolves against Requirement's `acceptance_criterion:` namespace; whole-Requirement remains the default; **per-relationship-schema opt-in discipline** — each relationship-type schema explicitly permits or rejects each primitive) | [ADR/0021 §"Decision §6"](ADR/0021-relationship-types-v-and-v.md) | `verifies` (target-side opt-in), `cites` (source-side opt-in); `tested_against` and `satisfies` do NOT opt in in this bundle (reserved for future opt-ins via Schema Change Notes — `satisfies` retrofit explicitly deferred per ADR/0021 §6) | Schema-rejects dangling `acceptance_criterion:<id>` references against the resolved Requirement Revision's `acceptance_criterion:` namespace. Whole-Requirement and criterion-scoped claims for the same Requirement-Pair coexist; tooling MAY surface as redundant but neither schema-rejected. Reopens [ADR/0009 §"Alternatives §E"](ADR/0009-relationship-type-satisfies.md) deferral under the condition ADR/0009 named (V&V taxonomy now exists). The primitive being defined at the trace-relationship base schema level does NOT retrofit `satisfies` — explicit per-relationship-schema opt-in required. |
| Cross-project per-type asymmetry across the trace-relationship family (target-Type-governs precedent operationalized per-type with explicit rationale dimensions) | [ADR/0009 §3](ADR/0009-relationship-type-satisfies.md), [ADR/0013 §"Cross-project"](ADR/0013-relationship-type-allocates-to.md), [ADR/0018 §"Decision"](ADR/0018-relationship-type-depicts.md), [ADR/0021 §§2-4](ADR/0021-relationship-types-v-and-v.md), [ADR/0022 §7](ADR/0022-test-execution-model.md) | `satisfies` IN, `derived_from` / `refines` IN, `allocates_to` OUT, `depicts` OUT, `verifies` IN, `tested_against` OUT, `cites` IN (with explicit EvidenceArtifact-specific rationale), `executes` OUT, `executed_on` OUT, `produces` OUT (all three with explicit event-locality rationale per ADR/0022) | Per-type rationale dimensions are: local-approval semantics, Binding-Object-applicability, integrity anchoring, Float-vs-Fixed meaningfulness; ADR/0022 adds a fifth dimension for execution-instance relationships — **event-locality** (execution events are intrinsically local; cross-project authoring of a run that happened in another project's lab is structurally meaningless). Deliverable-vs-emergent governing principle informs but does not directly determine cross-project posture. Shape-only inference is rejected (per ADR/0013 / ADR/0021 Blocker 3 — explicit per-type case required when target Type differs in any of the four dimensions). EvidenceArtifact-specific (ADR/0021 §4): consumer's `cites` is the local approval act; no Binding Object needed; Revision content hash implicitly pins attachment-hash-chain integrity per S2 immutability; Float "current best evidence" semantic meaningful + Fixed for historical pinning. |
| Execution-instance relationship category (Fixed-only binding posture + unconditional Revision pinning + cross-project opt-OUT default + per-relationship-type cardinality-at-release with status-sensitive refinement) | [ADR/0022 §3-§8](ADR/0022-test-execution-model.md) | `executes`, `executed_on`, `produces` (future observation / measurement / event-recording relationships) | (a) **Fixed-only binding** (Float schema-rejected — execution records are historically anchored; "this past run tracks current Revision" is semantic nonsense); (b) **`endpoints[0].revision_id` REQUIRED** unconditionally (not Conditional); (c) **cross-project endpoints opt OUT** default (event-locality dimension reinforces target-Type-governs); (d) **per-relationship-type cardinality-at-release rules with status-sensitive refinement** (`executes` exactly 1 hard-fail; `executed_on` ≥1 hard-fail; `produces` ≥0 globally with `completed` SHOULD ≥1 diagnostic, `aborted` MAY 0, `inconclusive` MAY 0 or ≥1 — first introduction of status-sensitive cardinality); (e) **no `endpoints[].fact_ref` / `source_fact_ref`** (no Requirement endpoint — per-relationship-schema opt-in discipline from ADR/0021 §6 explicitly does not apply). Together these properties distinguish execution-instance relationships from design-intent trace relationships; future categories (observation / measurement / event-recording) inherit this posture wholesale. |

## 3. Coherence Checklist

Operational yes/no questions for arc-time review (Codex walks these against each proposal). Items earn their place either by (a) catching / nearly-catching an actual issue, or (b) being a load-bearing invariant the current work front will exercise repeatedly.

- **List-addressability:** Does any independently mutable list item carry annotations or need stable identity? If yes, does it have a stable `id`? ([S0 commitment 7](TruthModelSchema.md#7-list-addressability-rule); near-miss caught in [ADR/0011 Codex2](Discussions/20260518-11/Codex2.md).)
- **Released cross-Object geometry:** Does every released cross-Object geometric reference use `published_ref:<id>` unless `requires_validation` is explicitly invoked per the narrow exception? ([S3 commitment 11](TruthModelSchema.md#11-published-reference-ports-are-first-class-addressable-records-owned-by-objects).)
- **Engineering-structure cross-project:** Does the engineering-structure relationship target local Binding Objects unless [ADR/0008 §4](ADR/0008-cross-project-object-identity.md) narrow exception invoked? Catalog Part used as `composed_of` / `mated_to` endpoint must route through local Component. ([ADR/0010 §4](ADR/0010-relationship-type-composed-of.md), [ADR/0011 §6](ADR/0011-relationship-type-mated-to.md).)
- **Binding ownership:** Does Float / Fixed resolution have an explicit owner — relationship-level (direct-binding) or address-mechanism (indirect-binding)? Avoid ambiguous middles. ([ADR/0011 §5](ADR/0011-relationship-type-mated-to.md).)
- **Identity cross-check:** When endpoint `revision_id` is present alongside an address-resolved binding, does it match the resolved terminal Revision? Hard-fail on mismatch. ([ADR/0010 §3](ADR/0010-relationship-type-composed-of.md), [ADR/0011 §3](ADR/0011-relationship-type-mated-to.md).)
- **Released geometric satisfaction:** Does every released `mated_to` evaluate true against materialized occurrence transforms? Contradictory mates hard-fail. ([ADR/0011 §8](ADR/0011-relationship-type-mated-to.md).)
- **Canonical units at fact level:** Does every numeric engineering fact carry its unit at the field-name level (`_mm`, `_deg`) or schema-fixed canonical? No deferring units to "project policy" or "adapter convention." ([ADR/0010 §2](ADR/0010-relationship-type-composed-of.md), [ADR/0011 §2](ADR/0011-relationship-type-mated-to.md).)
- **Quaternion normalization:** Does every quaternion satisfy `|q|² ∈ [1 - 1e-6, 1 + 1e-6]`? No silent renormalize-on-read by adapters. ([ADR/0010 §2](ADR/0010-relationship-type-composed-of.md).)
- **AIADRA Core hosts nothing:** Does the proposal introduce any AIADRA-Core-operated service, live coordination, registry, or hosted federation? If yes, push back hard — this violates [Manifesto P11](Manifesto.md). Proactively-watched as cross-project / catalog / registry arcs approach. ([Manifesto P11](Manifesto.md); proactive per [Codex2 §2 meta-rule](Discussions/20260518-12/Codex2.md).)
- **Execution-record cardinality invariants:** For relationship categories with release-cardinality invariants (e.g., the execution-instance family from [ADR/0022](ADR/0022-test-execution-model.md)), are those invariants stated per relationship type and per status / lifecycle where needed? ([ADR/0022 §3-§5 + §8](ADR/0022-test-execution-model.md); earned via [Codex1 N1](Discussions/20260520/20260520-1/Codex1.md) catch on status-sensitive `produces` cardinality and [Codex2 N3](Discussions/20260520/20260520-1/Codex2.md) acceptance.)

## 4. Load-Bearing Now

- `undirected_constraint_graph` cycle policy operationally active ([ADR/0011](ADR/0011-relationship-type-mated-to.md)) — Layer-2 validator implementation will need mate-satisfaction evaluation at release.
- `acyclic_dependency` write-validation closure rule active ([ADR/0007 §5](ADR/0007-object-type-assembly.md), exercised by [ADR/0010](ADR/0010-relationship-type-composed-of.md)) — composition cycle check fires at commit AND release.
- Three serialization patterns coexist (source-anchored asymmetric binary; undirected multi-endpoint; pending asymmetric multi-endpoint). Next multi-endpoint ADR (`parameter_expression`) declares the fourth.
- Eight Codex-walkable invariants in the Coherence Checklist. Adding items follows the meta-rule.
- The Wedge becomes operationally accessible end-to-end after all framework + first three relationship-type ADRs ([ADR/0009](ADR/0009-relationship-type-satisfies.md), [ADR/0010](ADR/0010-relationship-type-composed-of.md), [ADR/0011](ADR/0011-relationship-type-mated-to.md)). Basic Wedge needs only `satisfies`; extended Wedge needs composition + mates.

## 5. Deferred / Do Not Accidentally Solve

- **Configuration / variants** — substantial future ADR. ([ADR/0007 §7](ADR/0007-object-type-assembly.md).)
- **Pattern primitives** (linear / circular / rectangular patterns) — future invariant binding any pattern primitive to per-occurrence addressability. ([ADR/0007 §8](ADR/0007-object-type-assembly.md).)
- **`lock` mate** — endpoint shape differs from feature-pair model; future Schema Change Note. ([ADR/0011 §2 / A1](ADR/0011-relationship-type-mated-to.md).)
- **Kinematic mates** (`gear`, `path`, `cam`, `universal`, `screw`) — future simulation / motion semantics layer. ([ADR/0011 §2 / A3](ADR/0011-relationship-type-mated-to.md).)
- **Criterion-level satisfaction** (`fact_ref` into Requirement `acceptance_criterion:`) — future Schema Change Note when verification taxonomy lands. ([ADR/0009 §2](ADR/0009-relationship-type-satisfies.md).)
- **Component target Type** for `composed_of` / `mated_to` — Component per-Type ADR or Schema Change Note. ([ADR/0010 §1](ADR/0010-relationship-type-composed-of.md), [ADR/0011 §1](ADR/0011-relationship-type-mated-to.md).)
- **Block allocation per Workspace** (Number Reservation Option 4) — Tier-L scale Schema Change Note. ([ADR/0004 §9](ADR/0004-number-allocation.md).)
- **Less-universal mates** (`symmetric`, `width`, profile mates) — Schema Change Notes when concrete production case surfaces.
- **Scale on `composed_of` occurrence** — future Schema Change Note if production case surfaces. ([ADR/0010 §2 / F1](ADR/0010-relationship-type-composed-of.md).)
- **Audit log shape** (failed-transaction retention) — [OQ-0003](OpenQuestions.md), deferred to Ring 2.
- **Project identity artifact** (the future `.aiadra/project-identity.yaml`-or-similar) — flagged but not designed. ([ADR/0008 §5](ADR/0008-cross-project-object-identity.md).)
- **Structured `step:` namespace on TestProcedure** — sidecar-level per-step records (id / description / expected_outcome / references); seed defers to attachment-only. ([ADR/0020 §"Alternatives §H"](ADR/0020-object-type-test-procedure.md).)
- **`test_kind` enum on TestProcedure** — Schema Change Note if recurring need surfaces. ([ADR/0020 §"Decision §2"](ADR/0020-object-type-test-procedure.md).)
- **Coverage / verification_state / evidence_ref properties on V&V relationships** — execution model now exists ([ADR/0022](ADR/0022-test-execution-model.md)) making aggregation derivable from graph traversal via the acceleration cache per [ADR/0001 §3](ADR/0001-storage-substrate.md); explicit aggregation properties still deferred per [ADR/0022 §"Decision §10"](ADR/0022-test-execution-model.md) carry-forward from [ADR/0021 Alternatives §G](ADR/0021-relationship-types-v-and-v.md) — wait for production case where derivation cost is unacceptable. Schema Change Note venue.
- **TestProcedure-as-source for `cites`** (procedure-cites-prerequisite-evidence — calibration certs required to execute) — still deferred per [ADR/0022 §"Decision §10"](ADR/0022-test-execution-model.md) carry-forward from [ADR/0021 Alternatives §C2](ADR/0021-relationship-types-v-and-v.md). The execution model handles procedure-produces-evidence (via `produces`); procedure-cites-prior-evidence is a distinct case. Future Schema Change Note.
- **Pass / fail outcome semantics** on V&V relationships — still deferred per [ADR/0019 §"Alternatives §E1"](ADR/0019-object-type-evidence-artifact.md) and [ADR/0022 §"Decision §10"](ADR/0022-test-execution-model.md) carry-forward. Outcome is a V&V judgment relative to a specific Requirement / acceptance criterion; same evidence can pass criterion A and fail criterion B. Belongs in V&V relationships when outcome framing eventually lands.
- **Test-campaign aggregation** (TestCampaign / VerificationActivity Object Type for grouping multiple TestExecutions into a certification suite) — deferred per [ADR/0022 §"Decision §10"](ADR/0022-test-execution-model.md). Each run is its own TestExecution; aggregation is graph-derivable for tooling. Future arc if the aggregation case becomes load-bearing.
- **Multiple parallel `source_authoring` posture for execution-instance Attachment-bearing Objects** — deferred per [ADR/0022 §"Alternatives §E3"](ADR/0022-test-execution-model.md). Seed adopts single-`source_authoring` template-preservation posture; multi-channel parallel observation captures (synchronized multi-instrument runs in safety-critical V&V) get a future Schema Change Note venue.
- **`executed_on` / `produces` source extension to Component / SoftwareModule** — deferred per [ADR/0022 §"Decision §10"](ADR/0022-test-execution-model.md), parity with [ADR/0021 §"Alternatives §B"](ADR/0021-relationship-types-v-and-v.md) `tested_against` source.
- **`satisfies` retrofit with criterion-level addressing** — `endpoints[].fact_ref` primitive exists at the trace-relationship base schema level after ADR/0021 but `satisfies` does NOT opt in; separate Schema Change Note venue when production case surfaces. ([ADR/0021 §"Decision §6"](ADR/0021-relationship-types-v-and-v.md).)
- **`tested_against` source Type extensions** — Component (B2) and SoftwareModule (B3); Schema Change Notes when concrete cases surface. ([ADR/0021 §"Alternatives §B"](ADR/0021-relationship-types-v-and-v.md).)
- **Verification-method consistency project-policy override mechanism** — Schema Change Note if recurring need to escalate from tooling-aided diagnostic to schema-enforced. ([ADR/0021 §"Decision §7"](ADR/0021-relationship-types-v-and-v.md).)
- **`derived_geometry_from`** — last unfilled named relationship type from [ADR/0009 §3](ADR/0009-relationship-type-satisfies.md); awaits FreeCAD Domain Adapter scope per [Manifesto P12](Manifesto.md).

## 6. Recent Pattern Changes

Rolling log; last 3-5 arcs; newest first.

- **Arc 20260531-1 (Phase 0 skeleton — [`aiadra-core/`](../aiadra-core/) per [ADR/0025 §1](ADR/0025-aiadra-core-runtime-scope.md)):** **Fourth code-producing arc; first non-spike code arc; first production-grade Python package landed.** Implements [ADR/0025 §1 Phase 0](ADR/0025-aiadra-core-runtime-scope.md) end-to-end at [`aiadra-core/`](../aiadra-core/). Package layout per [ADR/0025 §3](ADR/0025-aiadra-core-runtime-scope.md): `aiadra-core/` at AIADRA repo root parallel to `spikes/`; `src/aiadra_core/` with `truth_model/` (6 modules — atomic, sidecar, event_log validated iterator, reservation, revision with hash-from-disk, manifest with deterministic JSON) + `validation/` (4 modules — schema with bundle-index dispatch + `referencing.Registry` migration, profile with regex-grade lint + Profile-conformant dumper, fold with **bidirectional** sidecar/event invariant per Codex1 B3, digest with project-pin verification) + `transaction/boundary.py` (STUB; Phase 1 wires Draft-then-commit) + `vault/` (abstract `VaultAdapter` interface + content-addressed local-FS reference implementation) + `cli/` (`__main__` argparse dispatch + read-only `validate` + read-only `inspect` + 15 STUB commands for state-changing operations). **Three-round Claude↔Codex arc** (3+2+0 blockers; converged faster than spike code arcs because the canonical infrastructure was already proven in the Wedge spikes). Round 1: Codex caught 3 blockers (B1 fixture projects missing `.aiadra/schemas.yaml` so digest verification couldn't honestly run; B2 inspect target was wrong — `P-000058` not `P-000023`; B3 fold check was one-way — must reject extra on-disk sidecars not derivable from events). Round 2: Claude2 absorbed all 3 + implemented full 1,363 LOC Python; Codex caught 2 new blockers (B1 `aiadra inspect` didn't call `verify_project_pin` so pin-verification was asymmetric across read-only commands; B2 removing `event/_base.schema.json` to avoid `$ref` complexity conflicted with ADR/0003 explicit per-event-type shared-base decision). Round 3: Claude3 absorbed both — `aiadra inspect` now pin-verifies first (exit 3); `event/_base.schema.json` restored with per-event schemas using `allOf [{"$ref": "_base.schema.json"}]` + `unevaluatedProperties: false`; **validator migrated from deprecated `jsonschema.RefResolver` to canonical `referencing.Registry`** (the jsonschema 4.18+ path Codex N2 hinted would eliminate the deprecation warning — and it did). Codex3 signoff; 38 tests passing in 0.59s, no warnings. **Three Phase-0-internal friction items surfaced + resolved in-arc:** (a) carried spike schemas had `$id` naming OLD flat filenames — stripped from all 32 (bundle dispatch is index-based not $id-based); (b) cross-schema `$ref` initially required full `referencing.Registry` migration (B2 round-2 absorption); (c) `lint_profile()` is regex-only — duplicate-key detection requires `load_yaml()` for full Profile check. **Pattern-setting moves for future production-grade code arcs:** (a) **`referencing.Registry` over `RefResolver`** — pre-load every bundle schema by absolute file URI; inject `$id = schema_uri` into shallow-copy schema at validation time so relative `$ref`s resolve against the Registry; eliminates deprecation warning; (b) **bidirectional sidecar/event fold invariant** — every folded UUID has matching working sidecar AND every on-disk working.yaml UUID present in fold; one-way check would preserve known hole per ADR/0001 §4 "neither silently wins"; (c) **`verify_project_pin` BEFORE any artifact read across ALL CLI commands** (not just `validate`) — ADR/0003 §9 digest-before-read applies symmetrically; exit 3 on missing/mismatched pin; (d) **`allOf+$ref` + `unevaluatedProperties: false`** as the canonical per-event-type schema pattern (vs `additionalProperties: false` which doesn't compose with `allOf`); (e) **schema bundle `$id` stripping** — schemas dispatched via bundle index don't need `$id`; carrying old `$id`s from a previous flat layout breaks resolver path resolution. **No friction-item absorption beyond setup** per ADR/0025 §1 Phase 0 scope discipline — F3 / W1 / W2 land in Phase 1; F1 / F2 / W3 SCNs in Phases 2-4. **No schema bundle bump** (Phase 0 materializes the canonical v0.19.0 bundle layout; bundle stays v0.19.0; final digest `sha256:38596393b12253d19366b73322693a07f9873a84a24fe89d779dbb27e29e5111`). **No Pattern Catalogue row added** (Phase 0 lands runtime infrastructure; no new schema patterns). **No Coherence Checklist item added** (existing 10 items cover the load-bearing invariants this arc touches). Spike-arc strand stays closed; production-arc strand has Phase 0 landed; next is Phase 1 runtime-behavior arc per [ADR/0025 §1](ADR/0025-aiadra-core-runtime-scope.md).

- **Arc 20260530-4 ([ADR/0025](ADR/0025-aiadra-core-runtime-scope.md)):** **Third code-producing-direction ADR after [ADR/0023](ADR/0023-wedge-spike-scope-and-runtime.md) + [ADR/0024](ADR/0024-wedge-002-spike-scope.md).** Pins **production-grade `aiadra-core` runtime scope + shape + repo layout + posture + six friction-item absorptions + 5-phase implementation sequencing**, without writing any `aiadra-core/` code. Ten pinned decisions (scope-first ADR; library + thin CLI Python 3.12+; `aiadra-core/` parallel to `spikes/`; F1 `parameter_changed.new_fact_provenance`; F2 hybrid numeric-only `threshold_expression` primitive opt-in unit-REQUIRED no-free-text-parsing; F3 Draft-then-commit per state-changing CLI command with git commit as atomicity boundary; W1 staged-release with each stage as canonical Release Manifest + final_stage marker + dependency-closure not type-taxonomy; W2 `attach-file` as canonical Transaction emitting `<type>_changed` with `attachment_delta` payload no standalone attachment_* event family; W3 per-relationship-type schemas + bundle lookup-namespace keyed by existing `type` field no data migration; explicit out-of-scope deferrals). **Three firsts** (each pattern-setting): first production-grade direction ADR; **first ADR informed by combined friction logs from two prior spikes** (Wedge-001 + Wedge-002 friction logs jointly load-bearing); first ADR to pin a multi-arc implementation sequencing roadmap (Phase 0 skeleton → Phase 1 runtime-behavior+W1+W2 SCNs → Phase 2 F1 SCN → Phase 3 W3 SCN → Phase 4 F2 SCN; all five SCNs MINOR). **Plus a 13-row disposition table** covering every non-absorbed friction-log item (already-handled / skeleton-arc-acceptance / future-SCN / accepted-spike-only) per [Codex1 N1](Discussions/20260530/20260530-4/Codex1.md) — auditable. **No schema bundle bump** (meta-decision; bundle stays v0.19.0). **No Pattern Catalogue row added** (opt-in primitive pattern in Decision §5 reuses ADR/0021 §6 per-relationship-schema opt-in discipline; runtime shape is materialization of existing Layer 1/2 patterns). **No Coherence Checklist item added** (existing 10 items cover load-bearing invariants this ADR touches). Two-round arc on Claude↔Codex protocol — Codex1 caught 4 blockers + 3 non-blockers (B1 Decision §8 `attach-file` mutates working sidecar without canonical event recreates F1 fold-check bug class; B2 Decision §6 atomicity "byte-identical working tree" claim conflicts with mutable working sidecar from §8; B3 Decision §7 stage manifest-vs-Release-Manifest semantics blurry; B4 Decision §9 new `relationship_type` field is data migration not no-op); Claude2 absorbed B1+B2 jointly via **Draft-then-commit per state-changing CLI command** (every state-changing CLI invocation is a Transaction; in-memory draft → validate → atomic write + git commit; git commit IS the atomicity boundary; working tree byte-identical pre-commit; `attach-file` is a canonical Transaction emitting `<type>_changed` event with `attachment_delta` for Attachment-bearing Object Types; no standalone `attachment_*` event family per Codex's explicit guidance); B3 absorbed via each stage IS a canonical Release Manifest with `manifest_type: release` + optional `prior_stage_manifest_ref` + `stage_number` + `final_stage` fields; intermediate stages release their subsets honestly (validation_outcomes scoped to per-stage checks; cross-graph checks like V&V chain integrity and execution cardinality run final-stage only); stage dependency closure (not Object Type taxonomy) is the gating rule per [Codex1 Q2 answer](Discussions/20260530/20260530-4/Codex1.md); B4 absorbed via bundle-index dispatch on existing `type` field (no new field; no data migration; MINOR bump per [Codex1 Q3 answer](Discussions/20260530/20260530-4/Codex1.md)). Codex2 signed off with 3 non-blocking wording repairs (use "relationship schema-family / lookup namespace keyed by `type`" rather than new `artifact_kind` per [Codex2 N1](Discussions/20260530/20260530-4/Codex2.md); describe `threshold_expression.unit` mismatch as **Layer-2 hard-fail at release** rather than JSON-Schema rejection per [Codex2 N2](Discussions/20260530/20260530-4/Codex2.md); pin explicit `final_stage: true|false` field rather than absence-of-subsequent-stage per [Codex2 N3](Discussions/20260530/20260530-4/Codex2.md)) — all three applied to final ADR/0025 wording. **Pattern-setting moves for future production-grade ADRs:** (a) **Combined-spike-friction-informs-production** — Wedge-001 + Wedge-002 friction logs as joint load-bearing input; pattern-setting for any future spike-series → production-grade transition; (b) **Multi-arc implementation sequencing roadmap pinned in scope-first ADR** — 5-phase sequenced follow-up arcs; pattern-setting for any scope-first ADR with multiple downstream SCN/skeleton/runtime-behavior arcs; (c) **Disposition table for non-absorbed friction items** — makes "anything missing" auditable per Codex N1 pattern; pattern-setting for future production-grade ADRs that narrow scope from a larger friction surface; (d) **Draft-then-commit per state-changing CLI command** — operationalizes [ADR/0004 §6](ADR/0004-number-allocation.md) cross-artifact atomicity via per-CLI-command git commit boundary; pattern-setting for the runtime-behavior arc and all subsequent runtime-shape decisions. Combined Wedge-001 + Wedge-002 friction logs now FULLY ABSORBED into ADR/0025 directions; the production-grade conversation transitions from "informed by spikes" to "implementing the pinned direction." Spike-arc strand for the basic + V&V-instrumented Wedge substantively closes; production-arc strand opens with the 5-phase sequenced roadmap. Next natural step is Phase 0 skeleton arc per ADR/0025 Decision §1.

- **Arc 20260530-3 (Wedge-002 spike implementation):** **Third code-producing arc; first V&V chain in code; first Attachment-bearing pattern in code; first execution-instance posture in code; first spike-grade Vault Adapter.** Implements [ADR/0024](ADR/0024-wedge-002-spike-scope.md) end-to-end at [`spikes/wedge-002/`](../spikes/wedge-002/). **Four-round Claude↔Codex arc** (matches Wedge-001 pattern) — Codex caught 5+1+1+0 blockers: round 1 (B1 event_log fold generic `*_created` pattern; B2 carried Part/Requirement schemas widened for tested_against / cites; B3 relationship_created event payload widened `oneOf` all 7 V&V + satisfies schemas; B4 manifest object_number pattern widened `^(P|REQ|TST|TEX|EVD)-[0-9]{6}$`; B5 V&V chain integrity `executed_on` same-Part check + complete-chain requirement) + N1 (attachment_record_shared schema requires full ADR/0017 committed fields including media_type + vault_path); round 2 (B1 parameter→attachment lineage release-validated — schema's `contains` constraint guaranteed each parameter HAD an `attachment:<id>` entry but didn't verify the id resolved to a same-Revision attachment) + N1 (FRICTION_LOG `Docs/Docs/` typo); round 3 (B1 lineage validator broadened from EVD/TEX-only to all 3 Attachment-bearing types with two-tier scope per [ADR/0017 §2 D7-escape](ADR/0017-object-type-drawing.md) inheritance: universal `source_authoring` presence + derived-role chain termination for TST/EVD/TEX; lineage-specific parameter→attachment for EVD/TEX only since TestProcedure parameters have no `derived_from` discipline per [ADR/0020 §4](ADR/0020-object-type-test-procedure.md)); round 4 signoff. **Five firsts validated in code** (each pattern-setting): first V&V-instrumented spike implemented; first Attachment-bearing pattern in code (TestProcedure + EvidenceArtifact + TestExecution); first execution-instance posture in code (Fixed-only binding + per-relationship cardinality + status-sensitive `produces` rule + Fixed-at-authoring via upfront `--rev-id` predeclaration); first spike-grade Vault Adapter (per-project local-FS content-addressed; idempotent; integrity-checking; parameter→attachment lineage + derived-chain termination + ADR/0017 `source_authoring` invariant all release-validated); first scope decision INFORMED by prior spike's friction log shaped the entire arc. **Pattern-setting for future code arcs:** (a) **two-tier validation scope** when Attachment-bearing template has type-specific divergences — universal checks span all types; lineage-specific checks scope to types with `derived_from` discipline; future Schema Change Note extension of the Attachment-bearing template should keep this scoping pattern; (b) **spike-local non-canonical helper files** (`.rev-id-map` + `.attachments-staging.yaml`) marked `artifact_kind: spike_local_*` and excluded from bundle index — pattern for ANY future spike helper file that isn't Product Truth; (c) **manifest authority model preservation across spike generations** — Wedge-002 manifest shape byte-compatible with Wedge-001 manifest shape; new validation invariants land as `validation_outcomes` entries, NOT new top-level manifest sections; this keeps the manifest schema stable across the spike series. **No schema bundle bump** (spike-grade implementation; bundle stays v0.19.0). **No Pattern Catalogue row added** (Wedge-002 implements existing patterns from ADRs 0017/0019/0020/0021/0022; doesn't introduce new schema patterns). **No Coherence Checklist item added** (execution-record-cardinality-invariants from arc 20260520-1 was exercised for the first time in code by this arc, validating its load-bearing role per Codex2/3/4 verification; no new item earns its place via the meta-rule). [`spikes/wedge-002/FRICTION_LOG.md`](../spikes/wedge-002/FRICTION_LOG.md) (with NEW §4 cross-spike comparison vs Wedge-001 + §5 round-by-round absorption notes) is the load-bearing input for production-grade `aiadra-core` runtime ADR. **Combined Wedge-001 + Wedge-002 friction logs now informs that production-grade conversation completely.** Spike-arc strand for the basic + V&V-instrumented Wedge substantively closes; next natural arcs are production-grade runtime ADR OR Schema Change Notes for the three load-bearing Wedge-001 friction items OR Wedge-003 with additional Object Types (Component / SoftwareModule / Drawing).
- **Arc 20260530-2 ([ADR/0024](ADR/0024-wedge-002-spike-scope.md)):** **Second code-producing-direction ADR.** Pins Wedge-002 (V&V-instrumented Wedge) spike scope, attachment handling, posture, repo layout, fixture strategy, execution-instance authoring discipline, deliverable shape — without writing spike code. Eight pinned decisions (scope-first ADR; full V&V chain; Fixed-at-authoring execution-instance discipline with upfront `--rev-id` predeclaration; Python 3.11+ throwaway; minimal local-FS content-addressed Vault Adapter at `outputs/vault/<sha256-hex>/bytes`; reuse ADR worked-example identities; parallel `spikes/wedge-002/`; carry Wedge-001 friction items F1/F2/F3 forward unchanged). **Five firsts** (each pattern-setting): first V&V-instrumented spike scope; first Attachment-bearing pattern landed in code (TestProcedure + EvidenceArtifact + TestExecution); first execution-instance posture landed in code (Fixed-only binding + per-relationship cardinality-at-release + status-sensitive `produces` rule); first spike-grade Vault Adapter (per-project local-FS content-addressed; idempotent; integrity-checking); **first scope ADR INFORMED by a prior spike's friction log** — pattern-setting for future scope ADRs in the spike series; Wedge-001 FRICTION_LOG.md shaped Decision §7 (carry forward vs Schema-Change-Note-inline). **No schema bundle bump** (meta-decision; bundle stays v0.19.0). **No Pattern Catalogue row added** (Wedge-002 implements existing patterns from ADRs 0017 / 0019 / 0020 / 0021 / 0022; doesn't introduce new ones). **No Coherence Checklist item added** (execution-record-cardinality-invariants checklist item from arc 20260520-1 is exercised for the first time in code by this spike, validating the item's load-bearing role). Two-round arc on Claude↔Codex protocol — Codex1 caught three blockers (B1 TestExecution canonical field names drift from [ADR/0022 §9](ADR/0022-test-execution-model.md); B2 execution-instance Float-then-materialize masquerade vs [ADR/0022 §6](ADR/0022-test-execution-model.md) Fixed-only requirement; B3 implied `attachment_*` event family outside scope); all three absorbed in [Claude2](Discussions/20260530/20260530-2/Claude2.md) per Codex stated repairs ([Decision §2.5](ADR/0024-wedge-002-spike-scope.md) execution-instance authoring discipline with upfront `--rev-id` predeclaration; canonical `executed_on_date` / `execution_status` / `operator_identifier` field names with explicit CLI-to-sidecar flag mapping; `attach-file` clarified as workspace/Vault helper with no canonical event). [Codex2](Discussions/20260530/20260530-2/Codex2.md) signed off with two non-blockers (validation-outcome count wording avoided; `.rev-id-map` declared spike-local non-canonical) absorbed into final ADR/0024 wording. **Manifest authority model unchanged from Wedge-001** — attachment hashes transitively pinned via Revision content; `attachment_integrity` lands as validation_outcomes entries; manifest shape byte-compatible with Wedge-001. **Criterion-level `fact_ref` opt-in** correctly located in `relationship_verifies.schema.json` (target-side) + `relationship_cites.schema.json` (source-side), NOT Requirement schema. Spec-arc strand stays closed; spike-arc strand reopens with Wedge-002. Next natural step is the Wedge-002 spike-writing arc that implements ADR/0024.
- **Arc 20260530-1 (Wedge-001 spike implementation):** **First code-producing arc in AIADRA's history.** Implements [ADR/0023](ADR/0023-wedge-spike-scope-and-runtime.md) end-to-end at [`spikes/wedge-001/`](../spikes/wedge-001/). Eight Python files + 8 JSON Schemas + bundle index + Reservations / sidecars / events / Revisions / Release Manifest as concrete artifacts; 12 AIADRA-YAML-Profile negative fixtures with proof-of-rejection. **First 4-round Claude↔Codex arc** under the protocol from arc 20260519-1 — Codex1 caught 4 schema-shape blockers (B1 satisfies record shape; B2 Requirement canonical shape; B3 mandatory `schema_version` on events; B4 bundle `revision` lookup + manifest `manifest_type`); Codex2 caught 3 implementation-faithfulness blockers (Profile quoting on dump; Windows-newline-driven hash mismatch; schema validation not at every read); Codex3 caught 1 narrow remaining blocker (event sequencing read events unvalidated); Codex4 signoff. **First "fold check catches a real bug at runtime"** — `parameter_changed` event payload couldn't derive a `fact_provenance` mutation the implementation was making; spike-grade workaround in place, [ADR/0001 §4](ADR/0001-storage-substrate.md) sidecar/event invariant validated as load-bearing during implementation. **Pattern-setting moves for future code arcs:** (a) Claude1-as-design-with-schemas + Claude2-as-implementation-running-end-to-end rhythm (4-round absorption cycle is normal for first code arcs in a domain; Codex catches both spec-faithfulness AND implementation-faithfulness over multiple rounds); (b) **AIADRA YAML Profile enforcement at spike grade** — regex-over-text lint for Profile rules + force-quote dumper representer + per-rule negative-fixture proof; gaps (two-segment version vs float, lazy-import event-validator) honestly logged as production-grade signals in FRICTION_LOG.md; (c) **schema-validated load helpers** resolve `(artifact_kind, discriminator) → schema` via bundle index per [ADR/0003 §2](ADR/0003-schema-governance.md) — every read path uses one; (d) **hash-from-disk-bytes after write** (not from in-memory text) — guards against silent newline-translation bugs on Windows; release-time re-read verification adds defence-in-depth; (e) **friction log surfaces three load-bearing production-grade signals** for future Schema Change Notes or ADRs (extend `parameter_changed` event payload OR define fold rule for AI Action Protocol provenance; canonical threshold-expression primitive on `acceptance_criterion` OR tooling-aided posture; cross-artifact atomicity strategy — write-ahead log / git-commit-as-boundary / transaction-manager). **No schema bundle bump** (spike-grade implementation; no canonical artifact-kind / pattern changes). **No Pattern Catalogue row added** (spike implements existing patterns; doesn't introduce new ones). **No Coherence Checklist item added** (the meta-rule earns items via real catch / near-miss OR load-bearing invariant the current front will exercise; the `parameter_changed` fact_provenance bug was caught by an EXISTING checklist item (sidecar/event invariant) so the item earns no new sibling). **[OQ-0007](OpenQuestions.md) `under-investigation` → `resolved`** per ADR/0023 §4/§8 condition (spike ran end-to-end + friction log reviewed). Spike-arc strand for Ring 1+Wedge-001 substantively closes; next natural posture-shifts are Wedge-002 scope-first ADR (V&V-instrumented Wedge) OR production-grade `aiadra-core` runtime ADR OR three Schema Change Notes from FRICTION_LOG.md. Commit hash recorded in arc folder [CLOSED.md](Discussions/20260530/20260530-1/CLOSED.md).
- **Arc 20260520-2 ([ADR/0023](ADR/0023-wedge-spike-scope-and-runtime.md)):** **First code-producing-direction ADR** lands the meta-decision pinning scope + runtime + posture + repo layout + fixture strategy + CI / packaging posture + deliverable shape for the **Wedge-001 spike implementation**, without writing any spike code in this arc (the spike code lands in a follow-up arc that references ADR/0023's pinned scope). Six pinned decisions: scope-first ADR structure (spike code in follow-up arc); basic Wedge per ADRs 0005 / 0006 / 0009 (clarifies the Glossary singular-count shorthand into the minimum coherent artifact set without expanding scope ambition — still Tier-S; still one Part / one Requirement / one parameter / one Transaction); Python 3.11+ as spike-only runtime (NOT a production runtime commitment); explicit throwaway posture; `spikes/wedge-001/` repo layout at AIADRA root (not git-ignored); reuse ADR worked-example UUIDs / Numbers as **spike-local demo records** (Numbers are per-project namespaces per ADR/0004, so reuse does NOT reserve those Numbers against future real project data). Plus: no CI for spike; CLI + checked-in fixtures + ≤2-page friction log as deliverable (friction log is primary). **Partial clarification of [Glossary "Wedge"](Glossary.md) entry's singular-count shorthand** ("one sidecar / one event-log entry" → minimum coherent artifact set per ADRs 0005 / 0006 / 0009); Glossary v0.23 → v0.24; Manifesto framing unchanged. **Reopens [OQ-0007](OpenQuestions.md)** per its `accepted-as-unresolved` resurfacing rule; status transitions to `under-investigation`; `resolved` waits for spike-run + friction-log review. **AIADRA YAML Profile spike-grade acceptance criterion** explicitly named per Decision §3 (no anchors / aliases / merge keys / custom tags; duplicate-key rejection; quoted ambiguous scalars via token-level lint; JSON Schema validation at every read; enforcement is normative at spike grade, not optional). **No schema bundle bump** (meta-decision; bundle stays v0.19.0). **No Pattern Catalogue row added** (ADR/0023 introduces no schema patterns). Two-round arc on the Claude↔Codex protocol; [Codex1](Discussions/20260520/20260520-2/Codex1.md) caught three blockers (Release Manifest scope crossing [ADR/0001 §3](ADR/0001-storage-substrate.md) acceleration-cache-never-canonical + [ADR/0009 §5](ADR/0009-relationship-type-satisfies.md) `satisfies`-materializes-inside-source-Revision boundaries; rejected-transaction audit-log accidentally pre-empting [OQ-0003](OpenQuestions.md) deferral; "basic Wedge verbatim" conflict with the coherent artifact set requiring partial Glossary clarification) and three non-blockers (AIADRA YAML Profile checks explicit; OQ-0007 transition timing; spike-local fixture identities); all six absorbed in [Claude2](Discussions/20260520/20260520-2/Claude2.md) per Codex stated repairs; [Codex2](Discussions/20260520/20260520-2/Codex2.md) signed off without further objection. Methodology arc flag confirmed — coherent as methodology because the pattern-setting moves (Python-as-spike-only-not-production; throwaway-posture-via-explicit-`spikes/`-location; spike-local-fixture-ownership-via-per-project-Number-namespaces; AIADRA-YAML-Profile-enforcement-spike-grade) all flow from the spec → code transition. Spec-arc strand for Ring 1 catalogue work substantively closes; next arc is the spike-writing arc that implements Wedge-001 per ADR/0023.
- **Arc 20260520-1 ([ADR/0022](ADR/0022-test-execution-model.md)):** **Combined test-execution-model ADR** lands the ninth Object Type — **TestExecution** — plus three new execution-instance relationship types — `executes` (TestExecution → TestProcedure), `executed_on` (TestExecution → Part | Assembly), `produces` (TestExecution → EvidenceArtifact) — in one bundle per [ADR/0021](ADR/0021-relationship-types-v-and-v.md) / [ADR/0012](ADR/0012-relationship-types-derived-from-and-refines.md) combined-ADR precedent. **Fourth Attachment-bearing Object instance** (third reuse of [ADR/0017](ADR/0017-object-type-drawing.md) template) with TestExecution-specific `source_authoring` specialization (canonical record of execution event — raw instrument output / signed inspector report / automated test framework output / video log; metadata-only stub forbidden) AND parameter lineage discipline parallel to EvidenceArtifact. **First execution-instance Object Type** (distinct category from design-intent and Attachment-bearing canonical-record Types — records historical events rather than engineering intent). **First execution-instance relationship category** — Fixed-only binding (Float schema-rejected; pattern-setting departure from [ADR/0009 §"Decision §4"](ADR/0009-relationship-type-satisfies.md) Float-default for the design-intent family); `endpoints[0].revision_id` REQUIRED unconditionally; cross-project endpoints opt OUT (event-locality dimension extends the cross-project rationale framework to five dimensions). **First per-relationship-type cardinality-at-release rules with status-sensitive refinement** — `executes` exactly 1 hard-fail; `executed_on` ≥1 hard-fail; `produces` ≥0 globally with status-sensitive rule (`completed` SHOULD ≥1 diagnostic, `aborted` MAY 0, `inconclusive` MAY 0 or ≥1) — preserves inspection-only legitimate exception while diagnosing the central-promise case. **First "resolution-by-declining-to-add"** — ADR/0021's deferred Part / Assembly → EvidenceArtifact direct edge resolved by *not* adding a direct shape; multi-hop traversal `Part →tested_against→ TestProcedure ←executes← TestExecution →produces→ EvidenceArtifact` carries the linkage with full execution-instance binding. **Four partial supersessions** in frontmatter: ADR/0005 + ADR/0007 EvidenceArtifact-target half of `tested_against` (resolved by declining-to-add per Decision §3 / §5); ADR/0019 `collection_context` load-bearing role partially superseded (field remains as optional prose-only context; structural linkage now graph traversal); ADR/0020 §"Consequences" event-payload sketch (`test_execution_started` / `test_execution_completed`) promoted to Object event taxonomy (`test_execution_created` / `_changed` / `_released` / `_retired`). One new Pattern Catalogue row (execution-instance relationship category — consolidated per Codex2 N3 to single row covering Fixed-only binding + Revision pinning + cross-project opt-out + status-sensitive cardinality); one new Coherence Checklist item (Execution-record cardinality invariants — narrowly phrased per Codex2 N3). Bundle v0.18.0 → v0.19.0; Glossary v0.23. V&V framework now operationally executable end-to-end (chain *procedure verifies requirement → part tested against procedure → execution executes procedure on part producing evidence → requirement cites evidence* fully wired with atomic binding). Ring 1 catalogue work for the spec-arc strand substantively closes; natural next posture-shift is into Wedge spike-implementation (Track B — basic Wedge loop now schema-feasible end-to-end with V&V execution instrumentation; `derived_geometry_from` remains the only unfilled named relationship type, gated on FreeCAD Domain Adapter scope per [Manifesto P12](Manifesto.md)). Two-round arc on the Claude↔Codex protocol; Codex1 caught two blockers (worked-example `derived_secondary` lineage violation + parameter `derived_from` syntax drift) and three non-blockers (status-sensitive cardinality refinement + glossary typo + Pattern Catalogue consolidation); all five absorbed in Claude2 per Codex's stated repairs; Codex2 signed off without further objection.
- **Arc 20260519-11 ([ADR/0021](ADR/0021-relationship-types-v-and-v.md)):** **Combined V&V relationship-type ADR** lands three relationship types in one bundle — `verifies` (TestProcedure → Requirement), `tested_against` (Part / Assembly → TestProcedure), `cites` (Requirement → EvidenceArtifact). Closes V&V wiring for the V&V Object Type triad end-to-end. **First criterion-level addressing landing** (reopens ADR/0009 §"Alternatives §E" deferral under the condition ADR/0009 named — V&V taxonomy now exists): `endpoints[].fact_ref` primitive (target-side) and `source_fact_ref` primitive (relationship-record-level, for implicit-source relationships) defined at trace-relationship base schema level; **per-relationship-schema opt-in discipline** introduced as the new pattern (verifies opts in target-side, cites opts in source-side, tested_against/satisfies do NOT opt in). **Cross-project per-type asymmetry** operationally complete across trace family (verifies IN, tested_against OUT, cites IN with explicit EvidenceArtifact-specific rationale — local-approval semantics + no-Binding-Object justification + Revision-content-hash-implies-attachment-chain integrity per S2 immutability + Float-vs-Fixed posture). **Verification-method consistency pinned** as tooling-aided diagnostic only (not schema-enforced; per Manifesto P11 + "default" framing in ADR/0006 §7). **Three partial supersessions** in frontmatter: ADR/0005 + ADR/0007 EvidenceArtifact-target half of `tested_against` pre-declaration (deferred to test execution model — direct deliverable-to-evidence requires atomic procedure-revision + executed-run + produced-evidence + tested-Object binding, which `tested_against` alone can't do); ADR/0020 line 236 `cites` direction (was ambiguously TestProcedure-source AND ADR/0019-deferral-lifting; ADR/0021 pins Requirement-source + defers TestProcedure-source per Alternatives §C2). Two new Pattern Catalogue rows (criterion-level Requirement addressing; cross-project per-type asymmetry). Bundle v0.17.0 → v0.18.0; Glossary v0.22. Named relationship-type catalogue from ADR/0009 §3 now operationally complete except `derived_geometry_from` (awaits FreeCAD Domain Adapter scope per Manifesto P12). V&V Object Type triad fully wired end-to-end; chain *Part `tested_against` TestProcedure `verifies` Requirement `cites` EvidenceArtifact* now expressible without wrong-verb workarounds.
- **Arc 20260519-10 ([ADR/0020](ADR/0020-object-type-test-procedure.md)):** Eighth Object Type — **TestProcedure** — lands as the third Attachment-bearing Object Type after Drawing and EvidenceArtifact. **Second reuse of ADR/0017's template** with a different per-Type `source_authoring` specialization: canonical procedure document (signed test plan / SOP / test script / runbook / regulatory protocol; metadata-only stub forbidden). Parallels ADR/0019's specialization mechanism with different semantic content. TestProcedure `parameter:` records are nominal design facts (typical `fact_provenance.category: "human_input"`), NOT derived measurements — no `derived_from` lineage discipline (that lives on EvidenceArtifact only). Three of Part's seven namespaces + inherited `attachment:` + singleton `test_procedure:` block (snake_case — divergence from ADR/0006 line 396's run-together `testprocedure:` pre-declaration; pinned per ADR/0016 `software_module:` precedent). `test_procedure:` carries `title` (REQUIRED), `verification_method` (REQUIRED enum verbatim from [ADR/0006 §7](ADR/0006-object-type-requirement.md) — cross-check anchor for future `verifies` ADR), plus optional `applicability_summary` / `expected_outputs_summary` prose. **`allocates_to` target Type union additively extended** to include TestProcedure (final union: `Part | Assembly | Component | SoftwareModule | Drawing | TestProcedure`; EvidenceArtifact excluded per ADR/0019). **Deliverable-vs-emergent governing principle for `allocates_to` target inclusion now operational** across three ADRs (Drawing YES / EvidenceArtifact NO / TestProcedure YES); new Pattern Catalogue row records the principle. **Wrong-verb authoring guardrail introduced** — within "MAY be an allocates_to target," only procedure-deliverable Requirements may target TestProcedure (product Requirements await `verifies`); schema cannot enforce, tooling-aided diagnostic; this also serves as the negative-case template for the future V&V relationship-type ADR. **Partial supersession of [ADR/0006 §"Consequences" line 397](ADR/0006-object-type-requirement.md)** — canonical procedure body now lives in `source_authoring` attachment, not sidecar structured text; structured per-step records deferred to Schema Change Note. Bundle v0.16.0 → v0.17.0; Glossary v0.21; seed Object Type catalogue 7 → 8. All three Tier-2 Object Types from the Glossary candidate pool (Drawing / EvidenceArtifact / TestProcedure) now promoted; V&V Object Type triad complete pending V&V relationship-type ADR.
- **Arc 20260519-9 ([ADR/0019](ADR/0019-object-type-evidence-artifact.md)):** Seventh Object Type — **EvidenceArtifact** — lands as the second Attachment-bearing Object Type after Drawing. **First reuse of ADR/0017's Attachment-bearing template** — structural shape inherits verbatim; divergences are *semantic* (`source_authoring` specialized to require canonical evidence payload, not setup-only) and *additive* (every `parameter:` record carries `fact_provenance.derived_from` lineage to supporting `attachment:` records, reusing S1's existing field; full chain enforceable). **No relationship-type schema extensions in this bundle** — Requirement-to-EvidenceArtifact citation/verification deliberately deferred to the future V&V relationship-type ADR (`allocates_to` MUST NOT be used as a temporary stand-in; wrong verb). Singleton `evidence:` block with `summary` + `evidence_kind` enum (7 values incl. `other`); `collection_context` prose-only guardrail. Arc closed under Petre-authorized one-more-round-skip due to Codex token constraints; both Codex1 blockers structurally absorbed per Codex1's stated close conditions.
- **Arc 20260519-8 ([ADR/0018](ADR/0018-relationship-type-depicts.md)):** Eighth relationship type — **`depicts`** — lands as Drawing → Part | Assembly | Component trace. Closes the pre-declarations from [ADR/0005 §11](ADR/0005-object-type-part.md) and [ADR/0007 §11](ADR/0007-object-type-assembly.md). Pattern-following (inherits ADR/0009's thirteen base trace-relationship pattern fields) with **one pattern-setting addition: explicit `occurrence_context` sub-field** on occurrence-qualified target endpoints — first source-anchored asymmetric binary trace relationship where the source (Drawing) lacks implicit composition visibility into the target. Coupling strict (both `occurrence_context` and `occurrence_ref` present, or both absent — schema-rejected XOR). Released Drawing Revisions pin both `occurrence_context.revision_id` (Assembly composition-path authority) and terminal `endpoint.revision_id` (depicted Object's resolved Revision); terminal `object_uuid` cross-checks path resolution. Component target included with explicit authority guardrail: Drawing's `attachment.content_hash` and Component's upstream binding integrity are distinct anchors connected by the `depicts` trace link. Direct cross-project endpoints rejected (target-Type-governs per ADR/0013). Pattern Catalogue Applies-to edits on Source-anchored and Direct-binding rows. After this arc, the named relationship-type catalogue from [ADR/0009 §3](ADR/0009-relationship-type-satisfies.md) is operationally complete except `derived_geometry_from` and `verifies`.
- **Arc 20260519-7 ([ADR/0017](ADR/0017-object-type-drawing.md)):** Sixth Object Type — **Drawing** — lands as the first **Attachment-bearing Object** operationalization (counterpart to ADR/0014's first External pointer Object operationalization; the Promotion Rule's two named non-disqualifier patterns are both now operationalized). New `attachment:` namespace introduced as the template for future Attachment-bearing Types (EvidenceArtifact). **D7-escape made structural** rather than aspirational: every released Drawing MUST carry a `source_authoring` attachment role; derived attachments (`rendered_primary`, `derived_secondary`) carry explicit `derived_from_attachment_id` lineage terminating at the authored source. Algorithm-qualified `content_hash` (from ADR/0016) generalizes to attachment integrity anchors; `vault_path` is non-authoritative locator hint. Tightest namespace adoption yet (2 of Part's 7 + new `attachment:` + singleton `drawing:` block); `title_block_fields` carries an explicit canonical-metadata-wins guardrail to prevent shadow-spec drift. Additive endpoint extension to `allocates_to` (Drawing target added). `depicts` relationship type deferred to natural next arc; pre-declared shape in ADR/0005 §11 / ADR/0007 §11.
- **Arc 20260519-6 ([ADR/0016](ADR/0016-object-type-software-module.md)):** Fifth Object Type — **SoftwareModule** — lands as the second Binding Object Type after Component. Validates the ADR/0014 External pointer Object template with Git-flavored discriminator shapes. **First Object Type to invoke dual Promotion paths:** External pointer Object (Binding lifecycle) for four discriminators (`git_module`, `package_registry`, `aiadra_catalog`, `custom`); direct Promotion via basic capability test for `workspace_git` (first-party / in-Commonspace modules). **Algorithm-qualified hash convention pinned normatively** for the first time (`git-sha1:` / `git-sha256:` / `sha256:` prefix discipline); authoring-intent fields (`ref`, `version_spec`) explicitly separate from released-immutable identity (`commit_hash`, `tree_hash`, `resolved_version`, `artifact_hash`). Additive endpoint extensions to `allocates_to` and `parameter_expression`; `composed_of` participation deferred (software composition awaits a future relationship type rather than destabilizing ADR/0010's universal transform). Glossary `parameter_expression` "local Component" wording generalizes to "local Binding Object." Seed Object Type catalogue grows to five.
- **Arc 20260519-5 ([ADR/0015](ADR/0015-relationship-type-parameter-expression.md)):** Declared `parameter_expression` — the seventh relationship type and **first asymmetric multi-endpoint serialization** (counterpart to ADR/0011's undirected/symmetric `mated_to` shape). Closes the "Asymmetric multi-endpoint serialization | TBD when ADR lands" Pattern Catalogue placeholder open since ADR/0011. **First fact-level endpoint addressing** — parameter addresses (`<object_uuid>:parameter:<id>`) rather than Object addresses. **Output-owned source-anchoring uniformly:** skeleton-driven propagation lives on the downstream Object; Assembly-aggregate cases live on the Assembly (output parameter is on the Assembly itself); Assembly-computes-constituent forbidden until occurrence-local parameter model exists. Expression payload `formula_string` with explicit `variables[]` symbol-to-endpoint bijection (schema-enforced); no `allow_unused` escape hatch; `language` is consumer-policy / adapter identifier (no Core enumeration per P11). Pattern Catalogue: three row edits (closes one placeholder; extends Indirect-binding + Engineering-structure direct-external-endpoint NO Applies-to to include `parameter_expression`).
- **Arc 20260519-4 ([ADR/0014](ADR/0014-object-type-component.md)):** First Object Type ADR since seed catalogue. **Component** lands as the canonical Binding Object Type for purchased / catalog items — first **External pointer Object** operationalization of the [Promotion Rule commitment 5](TruthModelSchema.md) named non-disqualifier pattern. Discriminator-specific upstream binding (`catalog_ref` / `supplier_ref` / `custom_ref`; `oneOf` schema) keeps non-AIADRA upstreams out of `project_scope`; mirrored `published_ref` carries `upstream_fact_ref` with release-time chain validation. **First arc to exercise AIADRA Core hosts nothing as a load-bearing Coherence Checklist item.** Closes three relationship-endpoint deferrals in the same bundle bump v0.10.0 → v0.11.0 — Component target for `composed_of` and `allocates_to`, Component endpoints for `mated_to`. Seed catalogue grows to four Object Types (Part, Requirement, Assembly, Component). Five of Part's seven namespaces adopted (no `feature:`, no `material:`); new singleton `component:` block.
- **Arc 20260519-3 ([ADR/0013](ADR/0013-relationship-type-allocates-to.md)):** Declared `allocates_to` (Requirement → Part / Assembly), closing the four-relationship trace family from [ADR/0006 §12](ADR/0006-object-type-requirement.md). Inherited [ADR/0009](ADR/0009-relationship-type-satisfies.md)'s thirteen base trace-relationship pattern fields *with one deliberate divergence:* **opted OUT of direct cross-project endpoints** because the target is a deliverable Object (Part/Assembly), not an external Requirement — the regulatory-clause justification doesn't transfer. Establishes the target-Type-governs-cross-project-policy precedent. Partial clarification of [ADR/0006 §C3](ADR/0006-object-type-requirement.md) line 109 wording (direction is Requirement → Part/Assembly per ADR/0006 §12; §C3's "subsystems (`allocates_to`)" reading clarified). No new Pattern Catalogue row; cycle class `trace_graph` already correct in ADR/0006 §12 (no supersession).
- **Arc 20260519-2 ([ADR/0012](ADR/0012-relationship-types-derived-from-and-refines.md)):** Declared `derived_from` and `refines` as intra-Requirement trace relationships in a combined ADR; inherited [ADR/0009](ADR/0009-relationship-type-satisfies.md)'s thirteen base trace-relationship pattern fields; no new pattern fields. Partial supersession of [ADR/0006 §"Decision 12"](ADR/0006-object-type-requirement.md) cycle-class rows (`acyclic_dependency` → `trace_graph` for these two relationships only). Pattern Catalogue gained one row (Requirement-to-Requirement trace relationships). First arc to run end-to-end on the Claude↔Codex coordination protocol from arc 20260519-1.
- **Arc 11 ([ADR/0011](ADR/0011-relationship-type-mated-to.md)):** Declared undirected multi-endpoint serialization, indirect-binding, multi-endpoint stable-ids patterns. Activated `undirected_constraint_graph` cycle policy. Superseded [ADR/0010 §3](ADR/0010-relationship-type-composed-of.md) worked-example placeholder `mated_to` shape.
- **Arc 10 ([ADR/0010](ADR/0010-relationship-type-composed-of.md)):** Declared transform shape (position + unit quaternion); canonical units at fact level (`position_mm`); binding-aware nested occurrence path resolution. Activated `acyclic_dependency` cycle policy. Superseded [ADR/0007 §2](ADR/0007-object-type-assembly.md) `relationship:<id>` prefix form.
- **Arc 9 ([ADR/0009](ADR/0009-relationship-type-satisfies.md)):** Declared first relationship-type schema with thirteen base pattern fields; trace-relationship direct-external-endpoint opt-in with Float external semantics.
