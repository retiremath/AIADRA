# SPDX / copyright audit (generated by make_package.py, read from SPDX tags in file content)

| file | SPDX |
|---|---|
| `src/binding/CMakeLists.txt` | AGPL-3.0-only |
| `src/binding/aiadra_solver.cpp` | AGPL-3.0-only |
| `src/extraction/src/App/planegcs/Constraints.cpp` | LGPL-2.1-or-later |
| `src/extraction/src/App/planegcs/Constraints.h` | LGPL-2.1-or-later |
| `src/extraction/src/App/planegcs/GCS.cpp` | LGPL-2.1-or-later |
| `src/extraction/src/App/planegcs/GCS.h` | LGPL-2.1-or-later |
| `src/extraction/src/App/planegcs/Geo.cpp` | LGPL-2.1-or-later |
| `src/extraction/src/App/planegcs/Geo.h` | LGPL-2.1-or-later |
| `src/extraction/src/App/planegcs/SubSystem.cpp` | LGPL-2.1-or-later |
| `src/extraction/src/App/planegcs/SubSystem.h` | LGPL-2.1-or-later |
| `src/extraction/src/App/planegcs/Util.h` | LGPL-2.1-or-later |
| `src/extraction/src/App/planegcs/qp_eq.cpp` | LGPL-2.1-or-later |
| `src/extraction/src/App/planegcs/qp_eq.h` | LGPL-2.1-or-later |
| `src/extraction/src/SketcherGlobal.h` | LGPL-2.1-or-later |
| `src/fetch_boost.py` | AGPL-3.0-only |
| `src/shims/Base/Console.h` | LGPL-2.1-or-later |
| `src/shims/FCConfig.h` | LGPL-2.1-or-later |
| `src/shims/FCGlobal.h` | LGPL-2.1-or-later |
| `src/shims/boost_graph_adjacency_list.hpp` | LGPL-2.1-or-later |

Vendored third-party trees (licensed as wholes, texts in dist/licenses/): src/eigen (MPL-2.0), src/pybind11 (BSD-3-Clause), src/boost after acquisition (BSL-1.0).

## Adoption addendum — arc 20260717-2 Gate F1 (2026-07-22)

The adopted production tree (`aiadra-solver/`) is the accepted SK-B package
plus the Codex16 B2 DLL-side compatibility handshake. Delta vs the accepted
audit above:

| file | SPDX | change |
|---|---|---|
| `src/handshake/aiadra_handshake.cpp` | LGPL-2.1-or-later | NEW — the DLL's own ABI/contract declaration export (licensed LGPL so the DLL stays a uniformly-LGPL artifact) |
| `src/binding/CMakeLists.txt` | AGPL-3.0-only | edited — handshake source added to the `planegcs` target |
| `src/binding/aiadra_solver.cpp` | AGPL-3.0-only | edited — `dll_handshake()` + `BINDING_ABI`/`BINDING_SOLVER_CONTRACT` binding identity surface; round 18 (Codex17 B1) adds `dll_origin()` (`GetModuleFileNameW` on the handshake `HMODULE` — the loader proves the handshaking module IS the selected artifact) |

Every other manifested file is byte-identical to the accepted package
(`package-manifest-accepted-gate2.txt`). Behavior neutrality of the delta is
proven by the frozen-harness corpus digest, reproduced exactly:
`061fbdec5913ee88943ac1241cc237dbd0075e121621b66be055f32befeeb736`.
